# 実装指示書: 新セッション立ち上げ ＋ 開店フェーズ 便①（文書とアセットの配置・素材差し替え）

作成: 2026-07-24 ／ 対象: **新しい Code セッション**
置き場所案: `brushup/gamelab-opening-step1.md`
起点: `origin/main` HEAD `c4abcda`。公開中は **v2.3-b6f**。

---

## §0. まず現在地を報告する（コード変更なし・報告のみ）

`CLAUDE.md` の指示どおり `progland-handoff.md` を読み、**以下を自分の言葉で報告**すること。ここで齟齬が出たら、実装に入る前に止めて確認する。

1. 現在の版と、直近で何が完了しているか
2. ゲームこうぼうの段階3が、どこまで進んでいるか（カードは何種か）
3. `npm run verify` が何を検証しているか（特に**ベースラインの意味**）

**⚠️ 台帳の「次にやること」は古い。** 台帳は「次＝区切り⑤ clone」と書いてあるが、**実際の次は本書の開店フェーズ**。§1 で台帳を書き換える。

---

## §1. 台帳の更新と、開店フェーズ文書の配置（文書のみ・単独 push）

### 1-1. 添付文書を `brushup/` に置く

| ファイル | 内容 |
|---|---|
| `gamelab-opening-design.md` | **開店フェーズの設計書（正本）**。全項目 神田さん承認済み |
| `gamelab-parent-guide.md` | 保護者ガイド `GAMELAB_GUIDE` の原稿（便④で使う） |
| `gamelab-asset-prompts.md` | アセット生成プロンプト一式（BGM 2本が未生成） |
| `gamelab-opening-step1.md` | 本書 |

### 1-2. 台帳を更新する

**b6e と b6f の状態**: 神田さんの簡易確認で**合格**。
ただし神田さんの方針として、**通しの実機確認は `brushup/cards-reference.md`（機能一覧）が揃ってからまとめて実施**する。したがって各便は簡易確認で先へ進む。

**「次にやること」を書き換える**:

```
次 = 開店フェーズ（brushup/gamelab-opening-design.md）
  便① 素材差し替え（本書）→ 便② マップ開店 → 便③ 教育接続 → 便④ ガイド＋BGM
  ※ 区切り⑤ clone は開店フェーズの後
```

**開店フェーズの確定事項を台帳に記録する**（設計書からの要約でよい）:
- マップの置き場所 = **じゅんびちゅう看板①（69.5, 34.13）を建物に差し替え**。看板②は残す
- 名前 = `short:"ゲーム"` / `place:"ゲームこうぼう"`
- 開放条件 = **なし**
- 教育接続 = XP 10 ／ マイルストーン5種 ／ **保存先は `gamelab.milestones`（スタジオと分離）**

---

## §2. 素材の差し替え（アイコン11枚・内観）

添付の画像を `src/assets/studio-assets/` に置く。**中身は1バイトも変更しない**。

| ファイル | 仕様 |
|---|---|
| `card_icon_19_random.png` 〜 `card_icon_29_toberu.png`（11枚） | 256×256 透過PNG |
| `gamelab-interior.webp` | 1600×900 / q75 / 76KB |
| `gamelab-building.png` | 256×256 透過PNG（**便②で使う。置くだけで配線しない**） |

すべて Chat が既存規約に合わせて加工済み（白フリンジの残り最大0.4%・既存18枚は中央値0.0%）。

### 2-1. アイコンの差し替え

`src/data/studio-blocks.js` の**暫定SVGグリフ11個をPNG import に置き換える**:

`iconMoveRand` `iconBounce` `iconScoreUp` `iconScoreDown` `iconBumpTarget` `iconDpad` `iconTapMove` `iconGoal` `iconChase` `iconFall` `iconJumpable`

既存18枚（`card_icon_01`〜`18`）と**同じ import の書き方に揃える**こと。差し替え後、暫定SVGを生成していたコードが未使用で残らないよう掃除する。

### 2-2. 内観の差し替え

`src/gamelab/mode.jsx` の `homeBg` を `gamelab-interior.webp` に変更する。
現在は**スタジオの内観を流用**しており、コードに「段階1は内装をスタジオ流用（色替え検討は段階3）」というコメントが残っているはず。**そのコメントも実態に合わせて更新**すること。

⚠️ **スタジオ側の `homeBg` は変更しない**（`studio-interior.webp` のまま）。

---

## §3. 変えないもの

- `DEFS` の中身・カードの並び・ラベル・色（**アイコンの絵だけが変わる**）
- `geometry.js` / `engine.js` / ベースライン
- こうぐだなの寸法・全画面のレイアウト・`OP_MS`・`CFG.MOVE_MS`
- **studio は完全無変化**（アイコン01〜18・内観・PALORDER 18種）
- マップ（`WorldMap.jsx`）には**まだ手を入れない**。建物の配置は便②

---

## §4. 検証

- `npm run verify` 全PASS。**`--update` は実行しない**。
  本件は画像アセットの差し替えのみで `DEFS` の中身は変わらないため、**ベースラインは1バイトも動かないはず**。動いたら**手を止めて報告**
- **deploy 前に必ずブラウザで表示を確認**（区切り①の TDZ の教訓。node の verify は実行時エラーを拾えない）
- **11枚のアイコンが棚に正しく出ていること**を実測して報告（型とアイコンの対応が入れ替わっていないか）
- ビルドサイズが極端に増えていないこと

### 実機ゲート（神田さん・iPad／studio・gamelab 両方）

1. こうぐだなの**11枚のアイコンが既存18枚と同じ質感**になっている
2. **アイコンとカードの対応が正しい**（じゅうじキーのカードに十字キーの絵、など）
3. こうぼうのカセットだな画面の**背景が工房の内観**に変わっている
4. **studio の内装と18枚のアイコンは変わっていない**

---

## §5. 中間報告ポイント

① §0 現在地の報告（コード変更なし）
② §1 台帳更新＋文書配置（**文書のみ・単独 push**）
③ §2 素材差し替え
→ 版を **v2.3-b6g** に上げる → **版上げ → build → deploy を一度で**
→ 本番反映を確認 → 台帳に「deploy済み・実機確認待ち」で記帳

各区切りで push して sha を報告。**Chat は git を触らない**（読み取り検証のみ）。
