# 正本ドキュメント修正パッチ（既存プロジェクト文書の書き換え箇所）

作成: 2026-07-07 ／ 目的: 今回の決定（着せ替え=主人公・バトル舞台=ジャングル/だいち等）が
既存の正本ドキュメントに未反映で、矛盾したまま残っている箇所を修正する。
**この修正は神田さんまたはCodeが、実際のリポジトリ内ファイルに対して直接適用すること
（Chatはproject knowledgeへの書き込み権限を持たない）。**

---

## 🔴 最優先: memo05_shop_implementation.md（Codeが今まさに着手する文書）

### 修正1: §1 段階2 の商品リスト

**現在の記述（削除）:**
```
- きせかえ: あいぼうのかざり（シルクハット30／リボン40／サングラス50／きらきら60／
  おうかん80）＋バトルのぶたい（にじのぶたい50／うちゅうのぶたい50）
```

**修正後（差し替え）:**
```
- きせかえ: 主人公（探検家）の着せ替えアイテム8種（6スロット）＋バトルのぶたい2種。
  詳細・価格は `replacement_instructions_for_code.md` §B・`dressup_system_design.md`
  §4データ構造を参照。バトルのぶたいは「ジャングルのぶたい」「だいちのぶたい」
  （各50枚・`battle_stage_jungle_nature_prompts.md`参照）。
```

### 修正2: §4「やらないこと」の記述

**現在の記述（削除・これが一番危険）:**
```
- 商品リスト（段階2）の中身・価格・効果は変えない。入口に店内を足すだけ。
```

**修正後（差し替え）:**
```
- 商品リスト（段階2）のうち「バトルのどうぐ」（4種の効果・価格・所持上限）は
  変えない。「きせかえ」は上記修正1のとおり主人公用に総入れ替えする
  （これは今回変更する対象。店の入口を足すだけ、ではない点に注意）。
```

---

## 🟡 p6_battle_shop_design.md

### 修正: §5-B コレクション表

**現在の記述:**
```
| 🎩 きせかえ | 相棒カードに付く飾り（ぼうし・リボン・メガネ等 5種程度） | 30〜80枚 |
| 🖼️ バトルのぶたい | バトル画面の背景（2〜3種） | 50枚 |
```

**修正後:**
```
| 🎩 きせかえ | 主人公（探検家）の着せ替え8種・6スロット（head/face/neck/
  chest/waist/back）。詳細は replacement_instructions_for_code.md 参照 | 種類による |
| 🖼️ バトルのぶたい | ジャングルのぶたい／だいちのぶたい（2種・既存の難易度別
  3枚=草原/夕日/夜とは別枠・そちらは変更なし） | 50枚 |
```

同ファイル §9「生成する絵」の「4.」も、相棒→主人公に読み替える旨を注記。

---

## 🟡 p6_implementation.md

### 修正: フェーズ2 ショップ section

**現在の記述:**
```
- きせかえ（非消耗・全品定価・ガチャなし）:
  - 相棒カードの飾り5種（30〜80）＋バトル背景2種（50）。
    当面は絵文字/自作SVGで実装（🎩🎀👓等）。反映先: 相棒カード＋バトル画面。
```

**修正後:**
```
- きせかえ（非消耗・全品定価・ガチャなし）:
  - 主人公（探検家）の着せ替え8種・6スロット＋バトル舞台2種（ジャングル/だいち）。
    画像は生成済み（shop_dressup_battle_items_20260707_v3.zip 参照）。
    反映先: 主人公が表示される全箇所（ヘッダー・ホーム・バトル画面・
    プロフィールカード等）。相棒カードではない点に注意。
```

---

## 🟡 feature-spec.md

### 修正1: §2 データ構造

**現在の記述:**
```
- profile: 名前・アバター・相棒選択・作成日
```

**修正後:**
```
- profile: 名前・character（"boy"|"girl"）・相棒選択・作成日・dressup
  （{head, face, neck, chest, waist, back} 各スロットの装備アイテムID）
  ※旧「アバター」（動物）は廃止。既存プロファイルの移行方法は
  replacement_instructions_for_code.md §A参照。
```

### 修正2: §1 全体構成

**現在の記述:**
```
- 起動 → プロファイル選択（最大4人・記録分離）… 済
  ／ BU予定: 「島の入口（港）」風に作り直し・交代動線を港に一本化（メモ01）
```

**修正後（1行追加）:**
```
- 起動 → プロファイル選択（最大4人・記録分離）… 済
  ／ BU予定: 「島の入口（港）」風に作り直し・交代動線を港に一本化（メモ01）
  ／ BU予定: 動物アバター選択→男女探検家キャラクター選択に置き換え
  （replacement_instructions_for_code.md §A参照）
```

### 修正3: §8 ショップ

**現在の記述:**
```
- きせかえ（あいぼうの飾り5種＋バトル背景2種・全品定価・ガチャなし）。
```

**修正後:**
```
- きせかえ（主人公の着せ替え8種・6スロット＋バトル舞台2種=ジャングル/だいち・
  全品定価・ガチャなし）。詳細は replacement_instructions_for_code.md 参照。
```

---

## 適用後の確認

上記パッチを適用したら、以下を再確認:
- `memo05_shop_implementation.md` を読んだだけのCodeが、相棒用の古い商品名
  （シルクハット等）を実装してしまわないか。
- `p6_battle_shop_design.md`・`p6_implementation.md`・`feature-spec.md` の
  3ファイルとも、「相棒」でなく「主人公」という言葉に置き換わっているか。
- 「にじ」「うちゅう」という単語がどのファイルにも残っていないか
  （`memo05_shop_implementation.md` に直接名前が出ていたのが一番の火種だった）。
