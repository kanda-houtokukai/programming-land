{
  "name": "programming-land",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify": "node tools/verify.mjs && node tools/verify-quiz.mjs && node tools/verify-typing.mjs",
    "deploy": "npm run verify && npm run build && rm -rf docs && cp -r dist docs && git add -A && git commit -m deploy && git push"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0"
  }
}
