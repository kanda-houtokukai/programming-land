# プログラミングランド v2 — 台帳（handoff）

最終更新: 2026-07-28（**v2.3-b6v ぶたいをフラットな床へ（案う）＋監査B-2 を是正＝deploy済み・⚠️実機確認待ち**。b6u も実機確認待ち。b6p（監査 A-1/B-1）も実機確認待ち。**b6q のボタン＋パネルは撤去済み**。便②(b6h)・便③(b6i)・FB便A(b6k)・FB便B(b6m)・**カバー絵(b6n)** は**実機OK**。便④-A（b6j）・段階3 区切り①〜④（b6b〜b6f・アーカイブ）・便①（b6g）は実機確認待ち。**次=b6p の実機確認**→ 残りは **便④-B（BGM・音源2本の生成待ちで着手できない）／監査の未対応分（下記）／カードのアニメーション（Chat が設計中）**。**区切り⑤ clone は開店フェーズの後**。実機確認は `brushup/cards-reference.md`（機能一覧）到達後にまとめて実施の方針＝各便は簡易確認で先行。段階3カード追加分=`brushup/gamelab-implementation-stage3.md`＋区切り④指示書 `brushup/stage3-step4-jumpable.md`＋カード一覧 `brushup/cards-reference.md`）

> 過去の版ごとの詳細ログは月別アーカイブへ（読むのは必要なときだけ）: **v2.3-b4e〜b6m** = `progland-archive-2026-07.md`（版のログのほか、**台帳から移した完了項目**もここ＝開店フェーズ確定事項／ゲームこうぼう段階A〜3の記録／完了フェーズの指示書／引退した未完了タスク） ／ **v2.3-b4d 以前**（＋過去フェーズの教訓の詳細）= `progland-handoff-archive.md`。

> ### ★台帳の維持規則（2026-07-24 制定／2026-07-25 改訂・全セッション厳守）
> **「今どこか」の版エントリは直近7版まで。** 8版目を足すときは、**最も古い1版を `progland-archive-YYYY-MM.md` へ移してから**追記する（原文のまま・要約しない。アーカイブは月ごとに分ける）。
>
> **★文字数の測り方（先に読む）**: 必ず**この形**で測る。
> ```bash
> LC_ALL=ja_JP.UTF-8 wc -m progland-handoff.md
> ```
> ⚠️ **素の `wc -m` を使わない。** この環境は locale が `C` のため、`wc -m` が**バイト数**を返す（＝`wc -c` と同値）。日本語では**約2倍**に出るので、達していないのに超過と誤判定する（2026-07-25 に実際に 32,500字を 59,681 と報告した事故あり）。
>
> **台帳が 3.5万文字を超えたら、まず「今どこか」の版エントリ数を数える。**
> - **8版以上** → 最も古い版をアーカイブへ**原文のまま**移す
> - **7版以下なのに超過** → 版ではなく**固定部（未完了タスク・生きている注意事項・ファイルの地図・決定事項）が育っている**。**完了・解決した項目を消すか、アーカイブへ移す**。生きている項目には手を付けない
> - **7版以下で、固定部にも消せる項目が無い** → 規則は守られている。重い版が続いただけ。**何もしない**
>
> ⚠️ **要約・圧縮による削減は禁止**（アーカイブは原文保存が目的）。どの分岐でも「短く書き直す」で減らさない。
> 全セッションが起動時に台帳を読むため、肥大はそのままコンテキストの浪費になる（2026-07-24 に 76,059字→26,930字へ分離した経緯）。
> ★保持する版 8→7（2026-07-26・しきい値 3.5万は据え置き）: 8版に減らしたときの想定は平均2,515字だったが、実際は**平均2,724字**まで伸び（版 21,792＋固定部 13,368＝35,160字）、固定部を削っても超過が残った。**前回と同じ考え方**＝記録は長くてよく、保持する数を減らすのが筋。
> ★保持する版 10→8（2026-07-25・しきい値 3.5万は据え置き）: 実測の内訳が**版 23,117字（10件・平均2,311）＋固定部 13,061字＝36,178字**。固定部はしきい値を決めたときの 12,847字とほぼ同じで、**超過の原因は版エントリが平均 1,965→2,311字に伸びたこと**だった。**しきい値を上げる案は採らない**（警告は消えても台帳が長いままで、毎セッション読まれるという元の問題が解決しないため）。**記録は長くてよく、保持する数を減らすのが筋**＝版エントリを短く書いて縮めるのは禁止事項（要約による削減）に当たるのでしない。
> ★しきい値 3.2万→3.5万（2026-07-25）: 実測の内訳が**版 19,653字＋固定部 12,847字＝32,500字**で、規則どおりでも 3.2万を超えて警告が出続ける状態だった。**誤警報は無視されるか、禁止したはずの原文圧縮を招く**ため、余裕を持たせた。

---

## 現在地サマリ（毎セッション冒頭にここだけ読む）

### 次にやること（★ここが最優先・カバー絵完了で更新 2026-07-25）

**次＝ゲームこうぼう 開店フェーズ**。正本＝`brushup/gamelab-opening-design.md`（全項目 神田さん承認済み）。便で分けて進める:

```
便④-B BGM … ⚠️音源2本が未生成＝着手できない（下の段取り参照）
※ 区切り⑤ clone（ぶんしんを だす）は開店フェーズの後に回す
```

**カードの動きを見せる「ためしてみる」**（案B・背景なし・3便に分割）:

```
★案B（ボタン→中央パネル）から 案A（ながおし中のふきだし内ループ）へ方針転換（2026-07-26）
  便①（b6q ボタン＋空パネル）は撤去済み。b6q の「ふきだしを棚の外へ」だけ残っている
「みぎへ」1種類でループ再生 … ✅完了 v2.3-b6r
始点ずれ修正＋残り11種の台本 … ✅完了 v2.3-b6s（12種）
うごきカテゴリの残り6種 … ✅完了 v2.3-b6t（19種）
実機FB2件（マス目の不揃い／とべるようにが止まる）… ✅完了 v2.3-b6u（deploy済み・⚠️実機確認待ち）
  ★次は実機で「動きが分かりやすいか」の判断
  ※ みため・おとは カテゴリまるごと無し＝混在しないので現状維持
指示書=brushup/card-animation-inline.md（正本）／設計=brushup/card-animation-design.md
  ※ brushup/card-animation.md は案Bの指示書＝§4の台本だけが生きている
```

★**完了した便（開店フェーズ①〜④-A・実機FB便A/B・カバー絵）の一覧は `progland-archive-2026-07.md` の「完了した便の一覧」へ移した**（2026-07-25〜26 に完了）。

- **便④-B（BGM）の段取り**: ⚠️**神田さんが Suno で2本生成するまで着手できない**（こうぼう用 `game workshop`／バトルアリーナ用 `battle arena`＝以前からの保留分。プロンプト=`brushup/gamelab-asset-prompts.md` §D・Custom mode・Instrumental オン）→ Chat が **-19.0 LUFS 正規化 → AAC .m4a 96kbps** に加工 → Code が `src/assets/bgm/` に置き、`bgm.js` の `SRC` と `App.jsx` の `TRACK` に接続。**`TRACK` には便②で `gamelab: null, // 便④で接続` の1行を用意済み＝そこを曲キーに差し替えるだけ**（既存11本と同じ手順）。
- ★**node で読めないファイル**（便③§0で確定・付与や成長まわりを触るとき必ず読む）: `growth.js` は `data/monsters.js`→`.png` の連鎖で **node から import できない**。これを import しているファイル（`studio/works.js`・**便③以降は `gamelab/works.js` も**）は**ブラウザ専用**で、`tools/` の verify から読ませてはいけない。node から試験したいロジックは共通核 `workshop/store.js` や `data/studio-blocks-defs.js` 側に置く（便③の `workUsesAnyType` はそうした）。
- 開店フェーズの関連文書（すべて brushup/）: 設計正本 `gamelab-opening-design.md`／保護者ガイド原稿 `gamelab-parent-guide.md`（便④で使う）／アセット生成プロンプト `gamelab-asset-prompts.md`（BGM 2本が未生成）／各便の指示書 `gamelab-opening-step1〜4a.md`（便①〜④-A はすべて完了）。
- ★**アセットの余白規約**（便②§0で確定・次に絵を足すとき必ず読む）: 256×256 の透過PNGで **`min(pad)=0`＝絵がキャンバスのどこか1辺に接している**こと。縦横比が正方形でない絵は、比が決める側の軸だけが0になる（「上下も左右も0」は成立しない）。守らないと `objectFit:contain` の枠に対して絵が小さくなり、マップ上で既存より小さく見える（便①で約7%の不具合が実際に発生）。詳細=`brushup/studio-map-placement.md`。

### 今どこか

- **公開URL: https://kanda-houtokukai.github.io/programming-land/**（リポジトリ kanda-houtokukai/programming-land）
- **設計書の版**: `feature-spec.md`・`roadmap.md` とも **b5h 時点へ追随済み**（2026-07-18・feature-spec に §10 つくるスタジオを新設＋§1/§2/§7-2/§9 を追随・roadmap を b5h 現在地へ全置換）
- **新モード「ゲームこうぼう」段階A〜段階3**: 設計正本=`brushup/gamelab-design.md`。**段階A〜段階3は完了**（残りは区切り⑤ clone のみ・開店フェーズの後）。段階ごとの記録と指示書の一覧は `progland-archive-2026-07.md` へ移した（維持規則の3つ目の分岐・2026-07-25）。
- **v2.3-b6p（2026-07-26・監査指摘 便①=かんとくベレー入手不能／cid のすり替わり＝⚠️実機確認待ち／deploy済み 1cd371c）**: 指示書=`brushup/audit-fix-1.md`（正本）・出典=`brushup/audit-report-2026-07-25.md`。★版は **b6o を飛ばした**（`0` と紛らわしい）。実装=3d2139d・版上げ=f1570d5（deploy=1cd371c）
  - **§1 A-1 かんとくベレーが永久に入手できなかった**: 解放判定が `Object.keys(DEFS)`＝**全29種**を要求していたが、studio の棚は `PALORDER` の**18種**だけ。差の11種（`scoreUp`/`scoreDown`/`moveRand`/`bounce`/`bumpTarget`/`dpad`/`tapMove`/`jumpable`/`chase`/`fall`/`goal`）は**すべて gamelab 専用で studio の作品に置けない**＝どうやっても達成できない条件だった（実測で確認）。**判定を `PALORDER.every(...)` に変更**（ラベル/flavor の「18」が元の仕様なので、ラベルではなく判定をそちらへ合わせた）。段階1で DEFS が18→20になった時点から壊れていた
  - **★§1-3 再発防止を verify に組み込んだ（可否の報告）**: この不具合は **verify 全PASS・UI も正常で、子どもが達成できないだけ**だった。`tools/verify-studio.mjs` に照合を追加＝①**判定が `PALORDER` 基準であること**（DEFS 全種へ戻したら FAIL）②**ラベル/flavor の数字が棚の種類数と一致すること**。⚠️`src/data/dressup.js` は画像を import するため **node から読めない**ので、**テキストとして読んで正規表現で照合**した（`gen-cards --check` と同じ考え方）。**わざと壊して両方が実際に FAIL することを確認済み**（ラベルを20に→FAIL／判定を DEFS に戻す→FAIL）
  - **§1-4 解放されるタイミング（確認）**: `checkAchievementUnlocks` は `App` の `update()` の中で走る＝studio は storage へ直接書くので、**こうぼう/スタジオから戻ったとき（`exitWorkshop` の再読込）**に拾われる。**実UIで確認**＝18種すべて使った状態でスタジオへ入って戻ると **「🎁 きせかえ「かんとくベレー」を てにいれた！」のトーストが出て `cosmetics.owned` に `head_kantoku` が入った**。1種でも足りないと解放されないことも確認
  - **§2 B-1 キャラを消して保存すると相手指定がすり替わる**: 保存が `cid` を捨て、読み込みが配列順で振り直していたため、途中のキャラを消すと `bumpTarget`/`goal`/`gameOver.targetId` が別のキャラを指した。b6d の `targetName` 救済でピルは「だれか」と出るのに**エンジンは一度も発火しない**＝「表示は正常なのに動かない」最悪の形だった
  - **§2 直し方（4点）**: (1) `doSaveWork` の scene に **`cid` を含める**（根治）(2) `loadScene` を **`c.cid || ("c" + (i+1))`**＝**cid が無い場合の挙動は1バイトも変えない**（みほんに cid が無く `other.key` が cid 由来＝トレースに直結）(3) `cidSeq` を **既存の最大値+1**（`chars.length` 起点だと c1,c3 の作品で c3 と衝突する。cid の無いデータでは max=chars.length ＝従来と同値）(4) 救済＝解決できない `bumpTarget`/`goal` の target → **`"any"`**／解決できない `gameOver.targetId` → **`gameOver=null`**。**`chase` は b6d のエンジン側の救済に任せて触らない**。★救済は**読み込み時のメモリ上だけ**で保存済みデータは書き換えない（次に保存すれば cid つきで直る）
  - **★§2 実測（実UI）**: 3体（c1/c2/c3・goal→c3・ばくだん=c3）を作り**真ん中の c2 を消して上書き保存**→開き直すと **cid は `c1,c3` のまま**・**ゴールのピルが「きのこちゃん」**（＝c3 のキャラ名。「だれか」に落ちていない）・**ばくだんの💀が c3 に付いたまま**。旧形式（cid 無し・target が実在しない）の作品を開くと **cid は従来どおり `c1,c2`**・ピルは**「だれか」**（＝`"any"` に落ちた）・**ばくだんタッチ ✕・💀ゼロ**（＝`gameOver` が null に落ちた）＝救済が意図どおり動作。studio のみほんも従来どおり `c1,c2` で開ける
  - **§2-3 スキーマと試験**: `SCHEMA_VERSION` **8→9**。`tools/test-roundtrip.mjs` に**11項目**追加（cid つきの往復完全一致／cid が振り直されない／**途中のキャラを消しても target が保たれる＝この不具合の再発検知**／cid 無しは従来どおり c1..cN／次の cid は最大値+1／cid 無しでは max=chars.length／救済4種＋chase 不変＋容器の中まで／gameOver→null）
  - 検証: **verify 9本全PASS（`--update` なし）**・★**`tools/studio-baseline.json` の SHA-256 が修正前と完全一致（`43b61933…869e`）＝トレース732イベント・パス98本・DEFS29種が1バイト不変**・本番 b6p（`index-sdcykg_l.js`）配信＋版表示 v2.3-b6p・アプリのコンソールエラーゼロ
  - ⚠️次: 神田さんの iPad 実機確認（§4実機ゲート6項目）: ①studio で18種すべて使うとかんとくベレーがもらえる②キャラを消して保存し直しても相手がずれない③ばくだんの💀が消えていない④古い作品を開いても壊れて見えない⑤みほん10本が b6n と同じに動く⑥studio が b6n と変わっていない
  - ✅通し実機確認で確認済み（2026-07-26）
- **v2.3-b6q（2026-07-26・カードの動きを見せる 便①=ふきだしに「ためしてみる」＋空のためすパネル＝⚠️実機確認待ち／deploy済み 6890d55）**: 指示書=`brushup/card-animation.md`・設計=`brushup/card-animation-design.md`（**案B・背景は出さない**＝神田さん承認）。文書配置=47478a5・実装=ab3707b（deploy=6890d55）
  - **★着手前に指示書の前提が崩れているのを検出して停止・報告した（案Aで再開）**: `.palDesc` は「押している間だけ表示」で、`onUp` が**無条件に `clearPalPending()`** を呼ぶ。指示書 §2-2 の「ボタンは ふきだしの最下段＝**そこへ届くには一度離す動きが要る**＝誤爆しにくい」という前提に対し、**その「一度離す」でふきだし自体が消える**。押したまま滑らせる経路も `setPointerCapture` と `PAL_DIR_LOCK=8`（8px でドラッグ判定）で塞がっていた＝**ボタンに到達する経路が存在しなかった**。合成ポインタで1周流して実測（押下260ms=表示／離した直後=null）
  - **§2 案A の実装**: `clearPalPending(keepDesc)` を新設し、**ながおしが成立したまま指を離したときだけ**（`onUp` の `wasArmed`）ふきだしを残す。★**ドラッグに入った場合は従来どおり即座に消す**（`startPalDrag` は `keepDesc` を渡さない＝三段導線の生命線）。消えるきっかけ=**外側タップ／別カードのながおし**（document の `pointerdown` で `.palDesc` の外なら消す）**／棚のスクロール**（`.palscroll` の `onScroll`）**／パネルを開く**。**ながおし前のタップで出る初回トースト「ながおしすると せつめいが でるよ」は従来どおり**
  - **§2-2 ドラッグ競合の回避**: **ふきだし本体は `pointer-events: none` のまま・`.tryBtn` だけ `auto`**（実測で `["none","auto"]` を確認）。ボタンはふきだしの最下段
  - **★あわせて ふきだしを「棚の外」へ出した（実測で見つけた新しい競合）**: 従来の x は `カードの右端+10` で、**2列の棚では左列を押すと右列のカードに重なる**。本体が `none` のうちは無害だったが、**ボタン(auto)が付き、離しても残るようになったため、重なった隣のカードが掴めなくなる**（実測: 「じゅうじキー」を押すと「**スコア －**」がボタンに覆われた）。x を `max(カードの右端+10, 棚の右端+8)` にして解消（修正後はボタン左端が常に 247 > 棚の右端 224・覆う棚カード0件）
  - **§2-3 パネル**: 中央に **400×250**（studio 実測）。中身=**空のアニメ枠**（「（ここに うごきが でるよ）」）＋`long`＋`desc`＋「とじる」。**とじる／外側タップ**の両方で閉じる・背後は既存の確認ダイアログと同じ暗さ・**開いたらふきだしは消える**。便①では**全29種に出す**（中身が空なので害がない・§2-4。便③で12種に絞る）
  - **★★§6 最重要の実測（棚の全カテゴリから指でドラッグ）**: 見出しを全部開いて**全カードを合成タッチでドラッグ**＝**gamelab 28/28・studio 18/18 成功**。**ふきだしを残した状態でも 28/28 成功**（新しい実機ゲート項目）。⚠️最初にみほんを開いた状態では「はた」「ゴール」の2枚が失敗したが、これは**きっかけは1キャラに各1本の既存のプルッ拒否**（空の作品で再測して 28/28）＝退行ではない
  - その他の実測: ながおし成立でふきだし表示→**ドラッグに入ると即消える**→ドラッグ後も出ない・置ける／タップで初回トースト／別カードのながおしで差し替わる（ランダムに うごく→おいかける）／外側タップで消える／ボタン 250×34
  - 検証: **verify 9本全PASS（`--update` なし＝ベースライン SHA-256 一致 `43b61933…869e`）**・本番 b6q（`index-Bvq0U9-n.js`）配信＋版表示 v2.3-b6q・「ためしてみる」「ここに うごきが でるよ」が本番バンドルに含まれることを確認・アプリのコンソールエラーゼロ
  - ⚠️次: 神田さんの iPad 実機確認（§6実機ゲート6項目＋追加1項目）: ①指で棚からカードをドラッグできる（**b5z からの退行がないか**）②ながおしで「ためしてみる」が見える③ボタンが押しやすい④パネルが「とじる」と外側タップで閉じる⑤パネルの大きさが適切⑥**ボタンを押すつもりがドラッグになる、が起きないか**⑦**せつめいが残っていても、次のカードを指でドラッグできる**。合格後 **便②（ミニステージで「みぎへ」1種類だけ動かす）**
  - ⚠️指示書 §2-2 は Chat が改訂して次便で差し替える（本便は会話での合意＝案A で実装）
- **v2.3-b6r（2026-07-26・カードの動き＝案A ながおし中のふきだし内ループ再生へ方針転換＝⚠️実機確認待ち／deploy済み 007a9ca）**: 指示書=`brushup/card-animation-inline.md`（正本）。指示書配置=2ccb895・実装=6b45540（deploy=007a9ca）
  - **★方針転換の記録（判断の経緯）**: カードの動き見本は **案B（ボタン→中央パネル・b6q）から 案A（ながおし中のループ）へ変更**。**実機の感触で神田さんが判断**（2段階の画面より長押しで自動ループが直感的）。**案Aを当初退けた理由「指でふきだしが隠れる」は、b6q の位置修正（ふきだしを棚の外へ）で解消済み**＝b6q は捨て実装ではなく、この案の前提を作った便だった
  - **§2 巻き戻し**: `.tryBtn`（とその `pointer-events:auto`）・中央パネル・`tryCard` の state を撤去。`clearPalPending` の `keepDesc` を廃し、**離したら常に消える b5x の挙動へ戻した**（ボタンが無くなれば「押している間だけ説明」の一貫性が戻る）。「残ったふきだしを消すきっかけ」3種の配線も不要になったので撤去。⚠️**初回トースト「ながおしすると せつめいが でるよ」は残す**（`wasArmed` はトースト側でも使うため判定は残した）
  - **§2-3 残したもの**: **ふきだしの位置 `Math.max(r.right + 10, palRight + 8)`**。b6q で実測して見つけた「2列の棚で左列を押すと右列のカードに重なる」への対処で、**この案でもアニメが隣のカードに被らないために必要**
  - **§3 入れたもの**: `.palDesc` の `long` と `desc` の**間**に `PalAnim` を追加。**背景は出さず、うっすらしたマス目だけ**（設計 §1・神田さん確定）。**`CharSprite` をそのまま縮小して使用**（座標・大きさ・表示状態だけで動き背景に依存しない）。ながおし成立で再生開始、**指を離すとふきだしごと外れて停止**（`useEffect` の後始末で `clearInterval` ＋ `eng.stop()`）
  - **★§3-3 エンジンは無改造で使えた（報告）**: node で先に実測＝`createEngine([{key,x,y,stacks}], cb)` に `hat → move×4` の台本を渡し、`start()` → `tick()`×4 で1マスずつ移動し `onDone(true)`。**`stop()` では位置が残り、`start()` が `resetChar` で初期位置へ戻す**ので、**ループは `stop()`→`start()` で作れる**＝エンジンに手を入れる必要がなかった。1周＝4拍×`TICK`400ms＝**1.6秒**（指示の1.5〜2秒に収まる）＋**周の間 300ms**（`PAL_ANIM_LOOP_GAP`）
  - **台本の持ち方**: `PAL_ANIM_SCRIPTS`（type → {beats, chars}）に**データとして**保持。**本便は `move`（みぎへ）1種類だけ**（§3-4）。ここに無いカードは**アニメ領域を出さない**＝従来どおり `long`＋`desc` のみ（実測: 「まわる」はアニメ無しで高さ89px）
  - **§3-1 ふきだしの高さ**: アニメ領域 `PAL_ANIM_H=150`（実測 250×152）。**台本のあるカードだけ**ふきだしが伸びるので、`y` のクランプを**有無で分けた**（アニメ有=`170+150+14`／無=従来の`170`）。実測でどちらも画面下端に収まる
  - ★`PAL_ANIM_H` / `PAL_ANIM_LOOP_GAP` は **`STUDIO_CSS` より前で宣言**（b6c の TDZ の教訓＝テンプレートリテラルが参照する定数を後ろに置くと画面が真っ黒になる）
  - **★§6 実測**: **棚の全カテゴリから指でドラッグ＝gamelab 28/28・studio 18/18・失敗ゼロ**（b6q と同じ検査）／**ながおしでアニメが出る**（キャラ1体・位置が 60→79→98→117→136 と4マス進み**頭の60へ戻ってループ**）／**ドラッグに入ると即消える**（三段導線の生命線）／**指を離すと消え、`.actor` も残らない**（再生が残り続けない）／**`.tryBtn`・`.tryPanel` はDOMに1つも無い**／**ふきだしが棚の外**（gamelab 左端210>棚202・studio 213>205・覆う棚カード0）／初回トーストも従来どおり
  - 検証: **verify 9本全PASS（`--update` なし＝ベースライン SHA-256 一致 `43b61933…869e`）**・本番 b6r（`index-Ci7eZzNs.js`）配信＋版表示 v2.3-b6r・**「ためしてみる」「ここに うごきが でるよ」が本番バンドルから消えていることを確認**・アプリのコンソールエラーゼロ
  - ⚠️次: 神田さんの iPad 実機確認（§6実機ゲート7項目）: ①ながおしでせつめいと一緒に動きが見える②**動きが分かりやすい（速さ・大きさ・ループの切れ目）**③指で隠れない④指を離すと消える⑤指でカードをドラッグできる（b5z/b6q から退行していないか）⑥ボタンとパネルが無くなっている⑦studio が壊れていない。合格後 **次便＝残り11種の台本を流し込む**（`card-animation.md` §4 の12種）
- **v2.3-b6s（2026-07-26・アニメの始点ずれ修正＋台本を12種へ＝⚠️実機確認待ち／deploy済み 32bc61c）**: 指示書=`brushup/card-animation-inline.md`（台本の一覧は `card-animation.md` §4）。実装=312b8d5（deploy=32bc61c）
  - **★始点ずれの原因（神田さんが特定・現物で確認）**: `cell` の初期値が **0** で、`useLayoutEffect` が実測するまでの**1フレームだけ** `ax = 22 + disp.x*0` になり、キャラが枠の**左上**に出てから `.actor` の `transition 340ms` で正しい位置へ動いていた＝**「左斜め下から始点へ動いてからループが始まる」の正体**。**`cell` が 0 の間は `CharSprite` を描かない**ことで解消（1フレームぶんなので最初から正しい位置に出る）。★`instant` で transition を切る案は**初回だけワープして見える**ので採らなかった
  - **台本を12種へ（`card-animation.md` §4・`move` は b6r で実装済み）**: すべて `PAL_ANIM_SCRIPTS` の**データ**として持つ（カードが増えたら1行足すだけ）。**★エンジンは無改造**で、既存の公開APIだけを使った:
    - 拍だけで動く7種: **くりかえし**（`repeat×3〔みぎへ〕`・のこり回数を数字で添える）／**ずっと**（`forever〔まわる〕`・止まらない＝くりかえしとの対比でリセットを挟まない）／**まつ**／**はねかえる**／**ランダム**／**おいかける**（2体・追われる側は `まつ` を挟んでゆっくり）／**ふってくる**
    - 入力が要る3種: **じゅうじキー**=`nudge()`＋**十字キーの絵**（押された向きが光る）／**タップいどう**=`bgTap()`＋`tapMoveStep()`＋**指の絵**／**とべるように**=`gravityStep()`＋`tryJump()`
    - 接触で光る2種: **ぶつかったら**／**ゴール**＝接触で発火した `spin` の `onFx` を合図に一瞬光らせる（きっかけが動いたことが見える）
  - **★実測で見つけて直した2件**: ①**タップいどう**が動かなかった＝**1拍目の `tapMove` カードが実行される前に `bgTap` して空振り**していた（`tapMovable` がまだ false）→「目的地が空いたら次をタップする」形へ変更 ②**ずっと**が止まって見えた＝`spin` は**座標が変わらない**ため、エディタ本体と同じ**演出クラスの付け替え（`spinA`/`stepA`/`hopA`）**を `PalAnim` にも入れる必要があった
  - **実測（13枚すべて）**: アニメが出るカード **13枚**（12種＋みぎへ）で**動かないもの0件・ふきだしが画面下端で見切れるもの0件**。**棚の全カテゴリから指でドラッグ＝28/28・失敗ゼロ**／ながおしでアニメが出る／**指を離すと消え `.actor` も残らない**（再生が残り続けない）
  - 検証: **verify 9本全PASS（`--update` なし＝ベースライン SHA-256 一致 `43b61933…869e`）**・本番 b6s（`index-C8W_EseP.js`）配信＋版表示 v2.3-b6s・アプリのコンソールエラーゼロ
  - ⚠️次: 神田さんの iPad 実機確認: ①**始点ずれが直っているか**（いきなり正しい位置から始まるか）②**12種それぞれの動きが分かりやすいか**（速さ・大きさ・ループの切れ目・十字キー/指の絵・光り方）③指でカードをドラッグできる④studio が壊れていない
- **v2.3-b6t（2026-07-26・うごきカテゴリの残り6種にも動き見本＝19種へ＝⚠️実機確認待ち／deploy済み 691495d）**: 指示書=`brushup/card-animation-move6.md`（正本）。指示書配置=c56873c・実装=5fa4cc7（deploy=691495d）
  - **★方針の変更（判断の経緯）**: 動き見本は「**文で伝わりにくい12種だけ**」の方針だったが、**うごきカテゴリで出るカードと出ないカードが混在**し、実機で「無い」と受け取られた（神田さんFB「ひだりへ から もとのばしょへ まで が無かった」）。**仕様どおりでも、隣り合ったカードで挙動が違うと子どもは「壊れている」と受け取る**。**カテゴリ単位で揃えることを優先**し、うごき6種（`moveL`/`moveU`/`moveD`/`spin`/`jump`/`home`）を追加して**19種**に。⚠️**みため・おとは カテゴリまるごと無し＝混在しない**ので現状維持（触っていない）
  - **追加6種**（すべて既存 `move` の雛形・新しい仕組みは不要）: `moveL` x:9→左へ4／`moveU` y:1→上へ4／`moveD` y:6→下へ4／`spin` n:2／`jump` n:2／`home` みぎへ×3→もとのばしょ
  - **★§2-1 の3点を実測で確認（報告）**: ①**`spin`=`spinA`・`jump`=`hopA` が付き、座標は不変**＝座標だけ見ると「動かない」と誤判定するところだった（b6s の「ずっと」で踏んだ落とし穴と同型）②**`moveU` は y が増える＝画面上へ**（`DIRS.moveU=[0,+1]`・y=0 が盤の下端）。実測で **うえへ ay −11.9→−59.6／したへ −71.5→−23.8** と正しい向き ③**`home` は 60→79→98→117 と3マス離れてから 60 へ戻る**ことを 0.1秒刻みで確認（粗いサンプリングだと往復を取り逃す）
  - **実測（19種すべて）**: アニメが出るカード **19枚**で**動かないもの0件・ふきだしが見切れるもの0件**。**棚の全カテゴリから指でドラッグ＝gamelab 28/28・studio 18/18・失敗ゼロ**。**studio でも うごき7枚すべてに出て混在なし**（みぎへ／ひだりへ／うえへ／したへ／まわる／ジャンプ／もとのばしょ＋せいぎょ3種＝10枚）
  - ★教訓（検査のやり方）: **カテゴリ見出しを盲目的にトグルすると閉じてしまい、見えているカードしか測れない**（1回目の通し検査が19→5枚になった）。**開いているかを見てから開く**こと。あわせて **b6f の教訓が再現**＝**プレビューペインが非表示だと `setTimeout` が抑制され**、長い検査スクリプトが終わらない（ペインを前に出すと完走した）
  - 検証: **verify 9本全PASS（`--update` なし＝ベースライン SHA-256 一致 `43b61933…869e`）**・本番 b6t（`index-ClptlqCq.js`）配信＋版表示 v2.3-b6t・アプリのコンソールエラーゼロ
  - ⚠️次: 神田さんの iPad 実機確認（§4実機ゲート7項目）: ①うごきカテゴリの全カードに動き見本が出る（混在の解消）②ひだりへ／うえへ／したへ の向き③まわる・ジャンプが動いて見える④もとのばしょの意味が伝わる⑤**ジャンプ と とべるように の違いが分かる**⑥指でカードをドラッグできる⑦studio でも同じように出る
- **v2.3-b6u（2026-07-26・実機FB=マス目の不揃い／とべるようにが止まって見える＝⚠️実機確認待ち／deploy済み f4fc990）**: 実装=fc5a4d6（deploy=f4fc990）
  - **【1】マス目が小さいカードがあった（神田さんが原因を特定）**: `.palDesc` は `max-width:250px` で **`width` の指定が無く、中身の文字数で幅が決まっていた**。`desc` が短い4枚（うえへ/したへ/まわる/ジャンプ＝**9〜13字**。他は27〜50字）でふきだしが細くなり、`width:100%` のアニメ枠＝**マス目も一緒に細く**なっていた。→ **アニメがあるときだけ `.palDesc.hasanim { width: 250px }`**（アニメ無しは従来どおり `max-width`）。**実測: 19種すべてアニメ幅250・高さ152の1種類のみ・見切れゼロ**
  - **【2】とべるように が「1マス下がるだけ」だった**: ご指摘の `jumpEvery` 7→3・開始 y:6→4 に加え、**調査で2つの真因**が出た（どちらも**台本側で解決・エンジンは無改造**）
    - **★① きっかけが尽きて待ち受けも無いとエンジンが自然終了する**（`running=false`）。以降 **`gravityStep` / `nudge` / `tapMoveStep` がいっさい効かない**。node で `[hat, jumpable]` と `[hat, jumpable, forever〔まつ〕]` を並べて再現＝前者は y=2 で凍結・後者は y=0 まで落ちて生存。→ **そうさ系3種（jumpable/dpad/tapMove）の末尾に `forever〔まつ〕`** を置いて生かす。★**じゅうじキー・タップいどうも同じ理由で途中から動かなくなっていた**（b6s の検査では初動だけ見て「動く」と判定していた＝**あわせて修復**）
    - **★② `tryJump` は `jumpable` だけでなく `operable` も要求する**（▲＝じゅうじキーで跳ぶ仕様）。→ **jumpable の台本に `dpad` も置く**。実機の遊び方（じゅうじキーの▲で跳ぶ）とも一致する
    - **実測**: ay **−76→−57→−38→−19→0（着地）→−19→−38→−57（頂点）→…→0（また着地）** ＝「**落ちる → 着地 → 跳ぶ → また落ちる**」が繰り返し見える（上がった6回・下がった9回／6秒間）
  - ★教訓: **「動いた」の判定は初動だけ見ると誤る**。b6s では じゅうじキー/タップいどう を「動く」と報告したが、**エンジンが自然終了したあと止まっていた**。長め（5〜6秒）に見て**周期が繰り返すか**まで確認すること
- **v2.3-b6v（2026-07-28・ぶたいをフラットな床へ＋監査B-2＝⚠️実機確認待ち／deploy済み 1a7b35d）**: 指示書=`brushup/stage-floor.md`（正本）。指示書配置=4eb5f28・床=c75dea5・B-2=6d562db（deploy=1a7b35d）
  - **§3 ぶたいの方針を「案う（フラットな床＋うっすらマス目）」で決着**（未完了タスク #7-① の決着）。違和感の正体は**「そらに立てる」こと** ── そうげんの絵は地平線が theater の**上から42.2%**、12×8のマス目は高さの**84〜91%**を占めるため、**y=5〜7 の3行が空の中**にあった。**gamelab だけ床にし、studio は従来の絵のまま**（スタジオの背景は「書き割り＝見るもの」／ゲームの背景は「どこに立てるかを答えるもの」で要求が違う）
  - **★案あ（上1/3を遠景＋下2/3に床）は取り下げ**: 上に空くのは **32px（7〜11%）だけ**で、遠景の帯にマス目が2〜3行食い込み解決しない。しかも ぶたいを高くする方向は**縦予算と正面衝突**する（未完了タスク #7-②）
  - **画像は1枚も作らず・差し替えず、CSSの色と模様のみ**。`src/assets/` の差分ゼロ＝バトルの絵はそのまま／おみせの `bg_jungle`・`bg_canyon`（各100コイン）も名前・価格そのまま
  - **★床の見た目は `data/studio-bgs.js` の `floorStyle(id)` 1関数に集約**し、**ステージ・ぶたい選択・作品サムネの3箇所すべてがそれを呼ぶ**＝id と色の対応が3箇所でずれる構造にしない。実測（computed color）= そうげん`140,193,82`／ジャングル`63,139,92`／だいち`180,92,51`／アリーナ`216,184,132`／スタジオ`168,124,74` の**5種すべて一致**
  - **★`StudioThumb` を本便に追加**（指示書の当初スコープ外・神田さん承認）: `.bgimg` は1箇所だが**背景を描く場所は他に3つ**あった。`.bgthumb`（ぶたい選択）は §3-1 が既出、みほんのたなは gamelab 全10種に `cover` 絵があり影響なし、**作品サムネ(`StudioThumb`)だけが漏れていた**。放置すると**カセットだなに絵のサムネが並ぶのに開くと床**になる。`WorkshopHome` から `isGame` を渡して解決
  - **§4 監査B-2 を是正**（未完了タスクから削除）: 1マスの式がキャラの大きさを勘定に入れておらず、**全画面**で上端の段の頭が切れ・右端の列が赤い幕に潜っていた。`src/workshop/stagefit.js`（新規・node安全）に `cellSize()` を新設し**式を1か所へ集約**、`WorkshopEditor` と `StudioThumb` の両方が呼ぶ
    - 新しい式: 横 `c ≤ (w−36)/(LCOLS−1+K)` ／ 縦 `c ≤ (h−12)/(LROWS−1+K*AR)`。**キャラが収まる条件をそのまま解いただけ**で恣意的な余白は足していない
    - **★`AR=724/543` が要る理由**: 監査の提案式は「キャラ画像は全て正方形」前提だったが、**主人公だけ縦横比1.333 で縦に長く律速になる**（てき・モンスターは正方形）。監査案のままだと全画面で **gamelab 約40px・studio 約56px 切れ残る**
    - **実測（iPad横 1024×768・全画面）**: gamelab ぶたい979×653 → 旧1マス76.1で頭**+75px**・右端**+31px** はみ出し／新68.2（**−10.4%**）で 0・−70px。studio ぶたい987×658 → 旧76.8で頭**+116px**・右端**+62px**／新65.0（**−15.3%**）で 0。**ぶたいは縮むが `ACTOR_K` は不変＝キャラと盤の比（手触り）は変わらない**
    - 監査の名指しの再現ケース（みほん「おちものキャッチ」のリンゴ役 `y:7`）を全画面で実測 → **切れゼロ**
  - **★実装中に落とし穴#8（宣言前参照のTDZ）を踏みかけた**: `BGS` に `floor` を生やしたが `FLOOR_TABLE` の宣言より前で、**node の verify では拾えない**（`studio-bgs.js` は画像importで node から読めない）。**`BGS` の形は不変のまま `floorStyle(id)` 経由に分離**して回避
  - 検証: **verify 9本全PASS（`--update` なし＝ベースライン SHA-256 一致 `43b61933…869e`）**・`src/assets/` 差分ゼロ・本番バンドル `index-BORHDNlC.js` に **v2.3-b6v** と `bgfloor` を確認（curl で実測）・studio は「ステージ=`.bgimg`・床とマス目は未生成・ぶたい選択3種すべて絵」を DOM で実測
  - ⚠️検証のため一時的にショップ絞り込みを外して5種を表示したが**戻して残骸ゼロを確認済み**。**セーブデータは一切書き換えていない**
  - ⚠️次: 神田さんの iPad 実機確認（§6 実機ゲート7項目）: ①床になり全マスに立てる②**5種の床が見分けられ寂しくないか（華やかさの最終判断）**③ぶたい選択のサムネと実際の床が一致④**スタジオは従来の絵のまま**⑤一番上の段のキャラの頭が切れていない（studio/gamelab 両方）⑥前に作った作品を開いてもエラーにならない（背景が変わるのは想定どおり）⑦カードの動き見本19種が b6u と同じ
  - 検証: **verify 9本全PASS（`--update` なし＝ベースライン SHA-256 一致 `43b61933…869e`）**・**棚の全カテゴリから指でドラッグ 28/28・失敗ゼロ**・本番 b6u（`index-BWJK6lgW.js`）配信＋版表示 v2.3-b6u・アプリのコンソールエラーゼロ
  - ⚠️次: 神田さんの iPad 実機確認: ①**19種すべてでマス目の大きさが揃っている**②**とべるように が「落ちる→着地→跳ぶ」を繰り返す**③**じゅうじキー・タップいどうも止まらずに動き続ける**（今回あわせて直った箇所）④指でカードをドラッグできる⑤studio が壊れていない
- 検証体制: `npm run verify` ＝ パズル162面（★3最短＋難易度カーブ）＋クイズ360問（正解一意＋難易度タグ照合＋ループ回数表記禁止）＋ローマ字128件。FAILだと `npm run deploy` で公開されない

### 未完了タスク（backlog・roadmap.md §2 と同期）

1. **通し実機確認の結果**（2026-07-26・v2.3-b6p）: **51項目中 43✅・❌ゼロ**
   - 未確認8件（**保存とごほうび5・ぶたい3**）は準備に時間がかかるため**普段使いで確認する**
   - 監査 **B-2 は b6v で是正済み**（1マスの式にキャラの大きさを入れた＝`src/workshop/stagefit.js`）。⚠️**ぶたいが gamelab −10%・studio −15%縮んだ**ので、実機で見え方を確認すること
2. **全体監査の未対応分**（2026-07-25 実施・報告書 `brushup/audit-report-2026-07-25.md`／対応便 `brushup/audit-fix-1.md`）
   - ✅**対応済み**: **A-1** かんとくベレーが入手不能／**B-1** cid のすり替わり（どちらも **b6p**）
   - ✅**対応済み**: **B-2** 全画面で上端の段の頭が切れ・右端の列が幕に潜る（**b6v**・式を `stagefit.js` へ集約）
   - ⬜ **D-2 通し実機確認が未実施** … **b6c/d/e/f/g/j/m の7版に実機記録が無い**。`brushup/cards-reference.md` 到達後にまとめて実施する方針の積み残し
   - ⬜ **C-2 ベースライン再生成が5回・うち1回が deploy コミットに埋没** … 中核値（トレース732・パス98本）は全期間一致を確認済みなので実害なし。再生成時は単独コミットにする運用へ
   - ⚠️ **台帳の「studio 完全無変化」は b6m 以降は成立しない**（ぶたいのショップ連動をスタジオにも適用したため・**意図的**）。以後この表現を使うときは範囲を明示すること
3. **メモ06-A Phase2 ナッジ**（タワーのレベル選択の後押し）… 保留（タワーの手応えが実機で落ち着いてから）
4. **クイズ深さ化** … 後回し（必要な帯だけ）
5. **App側進化演出との二重**（結果シーケンスの進化が山場になったので、二重がクドければ「バトル中はApp側を抑制」）… 実機判断
6. **高学年拡張**（未着手・着手前にChatで設計）: 帯B 変数・イベント／帯C 自由制作・デバッグ・コード橋渡し
7. **ゲームのせってい 再設計**（★設計・モック済み・未実装。b6a の申し送りが版エントリ内にしか無く、アーカイブ移動で消えるためここへ移設 2026-07-25）
   - モック: `brushup/gamecfg-mock1.html`（現行／案1 ルールカード／案2 めじるしタイルの比較・**案1 採用**）／`brushup/gamecfg-fit-sim.html`（右カラムの縦予算シミュレータ・1画面に収まるか判定つき）
   - ✅**①背景の方針は決着**（b6v・**案う=フラットな床**。詳細は b6v の版エントリ）。⚠️残る未決は**②縦予算**のみ（下記のとおり当時より悪化）
   - ★**床にしたことで縦予算は1pxも増えていない**（画像を足していないため）。②の検討は b6v の見た目を実機で確定させてから
   - ⚠️ **縦予算の再計算が必要**: シミュレータは じゅうじキーが無かった頃のもの。現在は `.dpadbox` 約144px（上演中・操作可能キャラがいるとき**通常画面にも出る**）＋ こうぐだな 24%→21%（b6a）でプレビュー約+12px。**当時の案1Cでも収まらない**。有力案＝**「上演中は `.gamecfg` を隠す」**（b6c で全画面に対してやったのと同じ理屈・追加UIが要らない）

### 次の一手

1. **神田さんの実機確認**（上記1の一覧）→ FBに応じて定数1箇所ずつ微調整
2. 更新の出し方: `npm run deploy` 一発（verify→build→docs/→push。devサーバは止めてから）。文書のみのコミットは手動 `git push origin main` まで
3. 調整値の場所: 経験値・レベル曲線・孵化=`src/growth.js` ／ バトル=`src/data/battle.js` ／ 結果シーケンス=`Battle.jsx` SEQ ／ クイズ生成=`tools/quizgen.mjs`（N・NAKAMA_ODD）＋素材=`tools/quiz-data.mjs` ／ パズル生成基準=`tools/criteria.mjs` ／ 島拠点座標=`Puzzle.jsx` ISLAND_POS ／ クイズひろば座標=`Quiz.jsx` PLAZA_POS

### ファイルの地図

| ファイル | 役割 |
|---|---|
| `progland-実装指示書.md` | **指示書の正本（第2版・2026-07-04改訂）**。第1版はgit履歴。承認なしに編集しない |
| （P2/P3/P4/P6 の完了フェーズの指示書6本） | 一覧は `progland-archive-2026-07.md` へ移設（2026-07-25） |
| `src/data/howto.js` | 各モード・島・カテゴリの「あそびかた」説明文 |
| `src/components/HowTo.jsx` | あそびかた折りたたみ部品（A4で既定閉じ＋「おしてね」誘導） |
| `src/components/blocks.jsx` | 積み木ブロック風の命令ブロック（パレット＋命令枠共通・くりかえしC字形・A5でパレットhighlight追加） |
| `tools/curve.mjs` | 難易度カーブ検証（par単調・段差・新概念1つずつ・verify統合） |
| `tools/curate.mjs` | 選抜つき生成（プール→par分布追従の均等選抜）＝量産の核 |
| `tools/solve.mjs` | ソルバー（最短par＋最短解sol抽出・メモ化） |
| `tools/gen-moshimo-easy.mjs` | もしも easy 9面の段階設計生成（A5）。`moshimo-段階設計-案.md`=承認済み設計 |
| `tools/gen-staged.mjs` | **段階設計セルの正本ジェネレータ（A6）**: 島3normal/hard・島4全・島5全の8セルを再生成（導入面＋回数キャップ＋parランプ）。⚠️`generate.mjs --write`全再生成は段階設計を失うので使わない |
| `src/data/badges.js` | バッジ26個の定義と自動判定（P5）。追加はここに `check(save)` を足す。既存IDは消さない（獲得済みは和集合で保持） |
| `tools/test-roundtrip.mjs` | 書き出し/読み込みの往復試験（localStorageシム）。セーブ項目を増やしたら必ずここに検証を足して実行 |
| `src/data/battle.js` | バトル設定の集約（敵9体・HP・かいしん率・XP・コイン/アイテム定義）。数値調整はここだけ |
| `src/components/Battle.jsx` | バトル画面（選択→シーン演出→勝敗）。演出はCSS keyframe＋段階制御 |
| `src/components/ParentGuide.jsx` ＋ `src/data/parent-guide.js` | 「おうちの方へ」モーダル（A7で全モード展開: パズル島1〜6・クイズ5カテゴリ・タイピング・おえかき）。**`src/data/parent-guide.js` が唯一の正本**（Chat原稿は取り込み済みで削除。キー名は原稿flow/stage1-3→実装yomitori/kotoba等に変換済み） |
| `quiz-書き起こし基準.md` | クイズ素材の人手作成基準（P6e以降は `tools/quiz-data.mjs` の素材に適用） |
| `tools/quiz-data.mjs` | クイズ素材の正本（因果チェーン・なかまわけタグ辞書・絵文字プール・フロー素材）。問題を増やす＝ここに素材を足して `node tools/quizgen.mjs --write` |
| `project-charter.md` | 設計書: 案件憲章（Chat側作成） |
| `education-curriculum.md` | 設計書: 学習カリキュラム設計（Chat側作成） |
| `roadmap.md` | 設計書: ロードマップ（Chat側作成） |
| `feature-spec.md` | 設計書: 機能仕様（Chat側作成） |
| `progland-handoff.md` | 本台帳（引き継ぎの入口はここ1本） |
| `progland-archive-2026-07.md` | 台帳アーカイブ（**b4e〜b5w の45版ログ**・2026-07-24分割。⚠️過去の記録＝現在の計画ではない。通常セッションでは読まない） |
| `progland-handoff-archive.md` | 台帳アーカイブ（b4d以前の版ログ・過去フェーズの教訓。⚠️過去の記録＝現在の計画ではない） |
| `CLAUDE.md` | Code用の案件前提・固有注意 |
| `brushup/` | **ブラッシュアップ設計パックの新規ドキュメント**（2026-07-06振り分け）。実装の波の地図=`03_code_guide_by_wave.md`（波ごとの使うファイル＋指示文）・`01_roadmap_implementation.md`・`02_handoff.md`、新規実装指示=memo04(おうち部屋)/memo05(ショップ)/worldmap_home_design。配置経緯は`brushup/_配置メモ.md`。※P6等の重複.md9本は既存日本語名ドキュメントが正本のため取り込まず |
| `brushup/shop_dressup/dressup_asset_baseline.md` | **着せ替え・アイテム作成の恒久基準**（画風・後処理・確定アンカー・微調整目安。実装値の正は`dressup.js`。§8=プロンプトファイル一覧）。同フォルダに生成プロンプト原本9点＋レイヤー図を収載（2026-07-08・原本は`~/progland-brushup/20260707_shop_dressup/docs/`）。⚠️恒久文書を`docs/`に置かない（deploy毎に`rm -rf docs`で消える）。★ベース人物は`character_base_prompts.md`=初期試作(無地・不使用)／`adventure_base_prompts.md`=探検家基本形2体(実採用・Chat復元)／`backpack_variant_prompts.md`=リュック4体、の3本に分かれる |
| （保管・リポジトリ外） | メモ08/09の統合待ち.js（`art_guide_revised.js`=第4波おえかき・`parent_intro.js`=第2波③理念文・`parent_guide_full.js`=参考）は `~/progland-brushup/js統合待ち/` に保管（Downloads一括削除の対象外・リポジトリ外）。波が来たら統合。新規画像 room-home.webp/shop-interior.webp は `src/assets/` へ配置済み |
| `programming-land.jsx` | v1移植元。参照専用・編集しない |
| `src/data/` | ステージ・クイズ・おえかき・バッジのデータ（ハードコード禁止の受け皿） |
| `src/workshop/stagefit.js` | **ぶたいの1マスの大きさの式（b6v・監査B-2）**。キャラが枠に収まる条件を解いた形。★ステージ本体と `StudioThumb` の両方がここを呼ぶ＝サムネが本物のミニチュアであり続ける |
| `src/engine.js` | パズル実行ルール（UIとソルバーの共通仕様） |
| `src/storage.js` | localStorage保存・プロファイル管理・書き出し/読み込み |
| `src/version.js` | バージョン表示（リリースごとに手で上げる） |
| `tools/verify.mjs` | ステージ全数検証ソルバー（`npm run verify`） |
| `docs/` | ビルド成果物（Pages配信元。`npm run deploy` が自動生成） |


### 決定事項（要点。詳細は指示書第2版を参照）

- [DECISION] Vite + React 静的ビルド。**配布はGitHub Pages公開**（2026-07-04に単一HTML配布から変更）。アカウント kanda-houtokukai・無料・push→Actions自動デプロイ・アプリ内バージョン表示
- [DECISION] **マルチプロファイル最大4人**。記録・相棒・バッジは人ごとに完全分離、書き出し/読み込みもプロファイル単位
- [DECISION] **育成ゲーム化**: オリジナルモンスター（実在IP禁止・自作SVG）。相棒3体から1体→経験値→3段階進化。島マップ・ずかん・クイズバトル（P6）
- [DECISION] 難易度3段階を全モード共通。記録は難易度別、解放条件は難易度内で完結
- [DECISION] フェーズ再編（第2版）: P0きばん → P1そだてる基盤 → P2パズル増量＋島マップ → P3クイズ増量 → P4タイピング → P5きろく・バッジ・ずかん拡張 → P6クイズバトル。**以後フェーズは番号＋名前で呼ぶ**
- [DECISION] P2ステージは手作業で作らず、ジェネレータ＋ソルバー自動検証（`npm run verify` 全数PASS）で量産
- [DECISION] やらないこと: オンライン対戦含む通信機能・広告・外部連携、サーバ・DB（拡張余地のみ残す）、実在キャラ素材/名前、UIトーン刷新
- [DECISION]（2026-07-08・メモ06-C）**コインは「進歩したぶんだけ」＝周回では増えない設計**。クイズ/バトル/タイピングも、パズル同様に new-best／初撃破／初クリアのみ付与へ統一（満点クイズ再挑戦=0・討伐済み敵の再戦=0・タイピング非更新の再挑戦=0）。日次上限は今回入れない（周回が止まるため）。将来アイテム増で稼ぎが不足したら**健全な順**に: ①新アイテムに新しい稼ぎどころ（新モード/新ステージ＝新★・初クリア）を一緒に付ける（憲章§4-9）→②既存マイルストーンの単価を上げる（`growth.js` の COIN 1か所）→③おえかきの日次上限を上げる（＝日次収入増・上限付きなので青天井にならない）→④最終手段: 進歩ベースを一部ゆるめる場合も「1日1回だけ再挑戦でコイン」等、必ず日次上限とセット。★**禁止**: 「周回で無制限に稼げる」に戻すこと。上限“付き”で増やすのはOK、上限“無し”に戻すのはNG


### 生きている注意事項

- ★ **文書のみのコミットは push されない**（push は `npm run deploy` が唯一の通常経路のため、docsだけのコミットはローカルに滞留する）。文書コミット後は必ず `git push origin main` まで実行し、raw URL で反映確認（2026-07-08、正本4ファイル差し替えコミットが未pushで版ズレ再発しかけた教訓）
- ★ **「プロジェクトナレッジ（Claude.aiプロジェクト）」と「リポジトリ（ローカル＋GitHub）」は別の入れ物で、自動同期しない**。両者の一致は手動（神田さんがプロジェクトナレッジへ手で配置）で保っている。2026-07-08の版ズレの真因は上記「文書のみのコミットのpush忘れ」＝リポジトリ内部の話であって、プロジェクトナレッジとの取り違えではなかった（プロジェクトナレッジは最初から07-07版で、GitHub版とbyte一致を確認済み）。この2つは別物、と前提を明記して混乱を防ぐ。なお Chat は raw.githubusercontent.com で GitHub の実ファイルを読め、プロジェクトナレッジ版との差分照合まで可能（版ズレ調査の手段）
- ★ dev サーバ稼働中に `npm run build` しない（ビルド破損の既知事故と同型）
- ★ AudioContext はユーザー操作後に初期化（v1は対策済み・踏襲）
- ★ 保存データはバージョン番号つき、`{...newSave(), ...parsed}` のデフォルト値マージ。形式変更時はマイグレーション同梱
- ★ プレビュー成功≠完成。フェーズ完了時は公開URLで実機確認（PC Safari＋タブレット）。Pages反映はpush後1〜2分・シークレットウィンドウで確認
- ★ セーブスキーマは相棒・経験値・ずかん・難易度別記録をP0から予約しておく
- ★ **向きの判定は CSS `@media` のみ。JS の向き検出（`matchMedia`／`ResizeObserver`／`orientation`）は使わない**（b4b の matchMedia 誤発火・b4l の ResizeObserver 不発火＝b5a で確立。バトルの横画面2カラム〔landscape かつ 820px以上〕が現行仕様）
- ★ **ゲームこうぼうの床の見た目は `data/studio-bgs.js` の `floorStyle(id)` が唯一の入口**（b6v）。ステージ・ぶたい選択・作品サムネの3箇所が呼ぶので、**ここを直せば3箇所そろって変わる**。逆に個別に色を書くと id と色の対応がずれる（verify では検出できない種類の事故）
- ★ **`BGS` に項目を生やさない**（b6v）。`STUDIO_CSS` と同じ TDZ の罠で、`studio-bgs.js` は画像importのため **node の verify では拾えない**。付帯情報は別テーブル＋関数で引く
- ★ **こうぼう/スタジオの「手触り」定数は `geometry.js` に集約。プロトタイプ由来の値を勝手に変えない**（磁石78/1.3/140・隙間180ms cubic-bezier(.25,.9,.35,1.25)・吸着110ms・掴み2.6deg/1.05・着地200ms＝b5d の移植基準。※チップは b5w で `G.CHIP` 34→30・`ICON` 26→24 に是正済み）。変更したら実機ゲート必須
- ★ **検証環境の癖**: プレビューペインが非描画だと rAF／setTimeout の拍が止まり、「音量が0のまま」「キャラが動かない」ように見える（b5k/b5l/b5r で観測・**実機フォアグラウンドでは起きない**）。ペインを前面にするか rAF を自前ポンプしてから判定する。dev サーバの接続断→自動リロードで `location.hash` が落ちることもある（開発環境事象・本番Pagesは無関係）
- 中間報告ポイント: P0完了時・P1完了時・P2ジェネレータ＋ソルバー完成時（量産前）・各フェーズ完了時

### 今も効く教訓（過去フェーズから集約。詳細はアーカイブ）

- ★ 背景＋%座標オーバーレイは「imgに高さを決めさせる」方式（display:block/width:100%/height:auto）が堅牢。`aspectRatio+cover` は幅依存でズレる（b2c）
- ★ ランダム選択・状態参照はコールバック/ループの外で1回（filter内のpickで毎アイテム再抽選=b4t／連打時のstale closureで上限超過=b3h／Reactのkeyに`Date.now()`はコンポーネント作り直し=P4）
- ★ 生成コンテンツは「機械が解けるか」だけでなく「人が図から一意に読めるか」も基準化（くりかえし図の回数表記禁止=P4後）
- ★ 部分改修は専用スクリプトで該当セルだけ差し替える（共有RNGの全再生成は下流まで変わる）。展開の前にまず診断して対象を絞る（A5/A6）
- ★ parは概念的な重さを表さない。重い入口は導入面（teach:true）で開ける（A5）
- ★ 実素材を見てから実装方法を確定する（robot.png回転の撤回=P3／透過素材は配置前にbbox確認=b4a）
- ★ 背景＋%座標に絵を置くときは**底基準アンカー**（bottom基準）で足元を地面に乗せる。中心アンカーは浮く（b3zの罠→b5b もりのシーンで確立）
- ★ **CSS の `width:100%` は img の inline width に負ける**。寸法をCSSに任せたい場所には inline size を渡さない（`.fitArt`／MonsterArt `size=null`＝b4f）
- ★ **TDZ でマウント時に白画面**（consoleは error boundary 警告のみで原因が出ない）。宣言より前を参照しないこと＝useEffect の deps に後方宣言の変数（b4n）／テンプレートリテラル内で参照する定数（段階3）。挿入位置は宣言順を守る
- ★ 一発アニメ（拒否のプルッ等）は React state 経由だと不安定 → `remove`→reflow→`add` の DOM 直接操作にする（b5d）
- ★ `git add -A` の巻き添え混入に注意＝add前に `git status`（zip混入=P3）
- ★ GitHub Pagesのデプロイはときどき失敗する→空コミットをpushして再デプロイ（P0）
- ★ バッジ等の獲得判定は「和集合」で保持（構成変更で獲得済みが消える事故防止=P2）
- ★ スキーマ移行を伴う変更は roundtrip に移行ケースを足して機械確認（b4f/b4j）
