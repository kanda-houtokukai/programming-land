# プログラミングランド v2 — 台帳（handoff）

最終更新: 2026-07-08（正本4ファイルを更新版に差し替え・版ズレ解消）

---

## 現在地サマリ（毎セッション冒頭にここだけ読む）

### 今どこか

- **公開URL: https://kanda-houtokukai.github.io/programming-land/**（リポジトリ kanda-houtokukai/programming-land）
- **v2.3-b3d まで公開済み（2026-07-07・第3波 着せ替え編 完了＋実機FB2点修正）**。実装完了の全体像:
  - **b3d 実機FB修正**: ①**四角い光る枠(roomhot)を廃止**→RPG風スピーチバブル(`.bubble`・下向きしっぽ付き)に統一（おうちの家具ラベル・ショップ店員ラベル両方）。タップ領域は透明化(`.tapzone`)。②**たんけんメガネのアンカー調整**（`dressup.js` face top 20.4%→16.5%・鼻〜口→目の高さへ）。男女とも実機（プレビュー）で確認・スクショOK
    - ⚠️**次セッションへの持ち越し**: 吹き出しの形・大きさの微調整／メガネの位置をあと1〜2%上下げ、は実機を見てから最終調整（神田さんの実機確認待ち）
  - **第3波 着せ替え編（v2.3-b3a〜b3c）＝主人公システム＋ショップRPG化（shop_dressup パック準拠）**:
    - **b3a ①動物アバター→男女探検家**: `storage.js`に`character`("boy"/"girl")+`dressup`(6スロット)追加・`createProfile(name,character)`に変更。新規=作成時に男女立ち絵2択（`ProfileCreate`）／**既存=次回ホームで一度だけ`CharacterSelect`**（相棒の初回選択と同パターン・データ完全引き継ぎ・以後変更しない）。**`PlayerAvatar.jsx`に表示を集約**（丸クロップicon/full立ち絵・移行前は旧絵文字フォールバック）＝置換6箇所（ヘッダー/選択カード/きろく/額縁/ParentHub×2）。旧AVATARS廃止。素材20点を`src/assets/`へ配置（**既存作法に準拠して機械リサイズ**: base6体=724px/着せ替え=512px/どうぐ=256px・計20MB→3.4MB）
    - **b3b ②ショップRPG化＋商品総入れ替え**: 店内=`shop-interior.webp`（**img駆動方式**）＋店員タップ→「いらっしゃい！ なにを かいにきた？」→どうぐ/きせかえ（serifは`Shop.jsx`のSERIF定数・memo05案）。もどる1階層ずつ。**きせかえ=旧・相棒の飾り5種を廃止**→`dressup.js`の`DRESSUP_ITEMS`（10種・anchor実測%・skillTie・flavor・acquire집約）。舞台=にじ/うちゅう構想→**ジャングル/だいち**（画像・各50）。どうぐ4種=絵文字→イラスト（効果価格不変）。買ったら即装備・同スロット1個・トグル。`equippedDeco`廃止・`equippedBgCss`→`equippedBgImg`。⚠️**旧きせかえ購入済みセーブ**: ownedに旧ID残存も表示されず装備は安全に無効化・**返金なし**（コイン調整と一緒に判断）
    - **b3c ③レイヤー合成＋実績解放**: `PlayerAvatar`内部に`ComposedBody`＝base（back装備で6体切替）＋5スロットをz順(waist→chest→neck→face→head)で合成（**呼び出し側無変更で全箇所反映**・icon/full両方）。実績2種=はかせぼうし（全162面クリア）/おうかん（バッジ全部）＝`checkAchievementUnlocks`（dressup.js）を`App.update`のバッジ判定直後で自動付与＋トースト。**ショップ棚にロックカード**（🔒＋条件文言＝P6fバッジ条件表示と同じ思想）。おうちの額縁=**全身立ち絵**（着せ替えの確認場所）
    - **正本文書パッチ適用**（`source_docs_patch.md`）: memo05/p6設計/p6実装指示書/feature-spec の旧仕様（相棒きせかえ・にじ/うちゅう）を修正。★**版ズレ調査の結果**: 正本4ファイル（charter/roadmap/curriculum/feature-spec）は**P2コミット(53523be)以降未更新**＝「正本更新」フォルダはローカルに来ておらず差し替え未実施。**Chatが正本更新4ファイルを書き出して神田さんへ渡す→受領後にコミット**（2026-07-07 Chat側で作業中）
    - **申し送り**: ⓐ着せ替え価格は仮（帽40/メガネ・バンダナ30/バッジ40/ベルト50/リュック60・舞台50＝`dressup.js`1箇所）ⓑ**メモ06-C（コイン価格調整・導入時付与見直し）は未着手**＝実機でコインの貯まり方を見てから ⓒアンカー・丸クロップは実機微調整前提 ⓓ女の子のバンダナと髪の前後は実機で要確認
  - --- 以下は第2波以前 ---
  - **第2波 段階③（v2.3-b2e）＝記録整理＋保護者向け総合案内「おうちのひとへ」（メモ09）＝第2波の最終段**:
    - 受領物配置: `parent_intro.js`→`src/data/parent-intro.js`（理念文）／`parent_guide_index.js`→`src/data/parent-index.js`（構成定義＝`GUIDE_SECTIONS`/`RECORDS_GUIDE`・**refキーを原稿版→実装キーに変換**: flow→yomitori／段階併記はkotoba/tanbun/bunshou）。※`parent_guide_full.js`は既存正本`src/data/parent-guide.js`（A7一本化）と内容同一のため**配置せず既存正本を参照**（本文の二重管理防止）。指示書は`brushup/`へ
    - ①**ヘッダー「きろく」ボタン削除**（`WorldMap`＋`common.jsx` onRecords削除）＝子ども記録の入口は机の日記のみ ②**子ども向け記録＝机の日記に一本化**（`Records.jsx`を子ども向けのみ＝プロフィール/相棒/バッジ。保護者向け%バー・グラフ・データ管理・確認モードは全てParentHubへ移設） ③**マップ最下部に「おうちのひとへ」帯**（マップ枠の外・保護者トーン・子ども導線と視覚区別）→保護者ゲート ④**総合案内 新設`ParentHub.jsx`**: **2タブ**〔📖このアプリについて（既定・理念ファースト）=理念文→モード一覧アコーディオン（初期全閉・複数同時開可・第2層項目タップで本文モーダル・タイピングは段階1-3併記・おえかきはkey:null直下解決）→きろくの見かた／📊きろくとデータ=**sticky「👀いま見ているのは○○さんのきろく」バー**（切替機能なし＝交代は港一本化維持）→学びの見える化→難易度別→14日→お子さんべつ→データ管理・確認モード〕。ダッシュボードの計算・ゲート判定は不変
    - **同時修正（実機FB）**: ★ヘッダー音量ボタンずれ修正（`common.jsx`＝タイトル空時minWidth無し＋名前ピル＋音量を1グループ化で音量の孤立折返しを解消）／ワールドマップ座標4箇所微調整（クイズ/おえかき/ちから top-1・タイピング left46→47＝1-3の調整を1pt戻し・375でtyping↔おうち-2pxだが視覚OK）／**mapfloatふわふわ振れ幅 -5px→-3px**（theme.js keyframe・周期/ディレイ/ロック静止は不変・部屋の相棒も同トーン）
    - **⚠️第2波 完了＝メモ01+04+09 完了**。おうち＝モーダルRPG部屋・交代＝港・記録＝子は机の日記/親はマップ下総合案内、に整理された
  - **第2波 段階②（v2.3-b2d）＝起動画面を港に刷新＋交代の港一本化（メモ01の核）**: 素材 `harbor-entrance.webp`（1600×900・起動背景）・`icon_harbor.png`（256px透過・港アイコン）を`src/assets/`へ配置。①**起動プロファイル選択画面を港に刷新**（`ProfileSelect.jsx`）: 背景=港を**cover**で全画面（この画面は絵連動タップ枠が無いためb2cのimg駆動方式は不要＝cover可）・カードにアバター＋名前＋**相棒の現在の姿**（`MonsterArt`＋`stageForLevel`）を大きく・桟橋デッキ（中央〜下部）に乗るよう配置・白フチ文字・◀もどるは置かない。データ/選択/作成/削除ロジックは不変。②**マップに「みなと」追加**（`WorldMap.jsx` `AREAS`に`{key:"harbor",short:"みなと",place:"みなと",small:true,left:9,top:86}`＝左下の海際・空き地3つは温存・`small`で50%表示）: ポップアップ「みなと へ いく」＋▶いく！→**既存`switchProfile`経路**（段階①の`setHome(null)`込み）で選択画面へ。誤タップ対策にポップアップを挟む。③**おうちから「じぶんを こうたいする」削除**（交代は港1本に）。375/768/1024でみなと重なりゼロ確認
  - **第2波 段階① 微修正（v2.3-b2b/b2c）**: おうち部屋のプロフィール（額縁）タップ枠が実機でズレた件。b2bは座標だけ直したが実機で再ズレ→★**根本原因は背景の表示方式**: `aspectRatio+objectFit:cover` は幅ごとにサブピクセルのトリミング量が変わり、絵の中の家具と%座標が端末幅ごとにズレる（ローカルで合わせても実機で外れる）。**b2cで表示方式を変更**＝背景imgに `display:block/width:100%/height:auto` でコンテナ高さを決めさせ、img自身を座標空間にした（タップ枠の%は常に絵と一致・トリミング/アスペクト計算に非依存）。額縁座標はグリッド実測で中心52/22に再調整（`HomeRoom.jsx` FURNITURE profile=52/22/12/16）。375/768/1024で一貫を確認。⚠️**教訓: 背景＋%座標オーバーレイは「imgに高さを決めさせる」方式が堅牢**（`aspectRatio+cover`は幅依存のズレ源）。※ワールドマップは旧方式のままだが画像がちょうど16:9で実機OK＝現状問題なし（ズレたら同方式へ）
  - **ブラッシュアップ 第2波 段階①（v2.3-b2a）＝おうちモーダル化＋RPG部屋化（メモ01の一部＋メモ04）**:
    - おうちを **App レベルのモーダルオーバーレイ**に（`home={from,open}` で呼び出し元を保持・画面は変えずオーバーレイ＝閉じると呼び出し元の状態ごと復元）。新規`HomeRoom.jsx`（room-home.webp背景・家具タップ領域＋`roomhot`うっすら光る演出・相棒を中央床に`mapfloat`で動的表示）。`MyHome.jsx`は役割をHomeRoomが継承し削除
    - **開く入口2つ（どちらもモーダル）**: ①全画面共通ヘッダーの名前パネル（`common.jsx`を`onOpenHome`対応・全10画面に`openHome`配線）②マップの家アイコン→「おうち へ いく」→モーダル（`WorldMap`で`myhome`だけ`onOpenHome`に分岐）
    - **家具割当**: 本棚=ずかん/机=きろく（`onEnter`で全画面へ）・額縁=プロフィール/相棒=詳細（部屋内ネストモーダル・画面遷移なし）・宝箱=「まだ なにも ないよ」（将来枠・座標保持）
    - **遷移の再配線（最重要）**: 本棚/机→全画面→◀もどるで**部屋に再オープン**→とじるで**呼び出し元へ**。App の`funcBack`が「おうち経由か否か」を判定（`home`非null＝部屋へ／null＝ヘッダーきろく等経由でマップへ）。Dex/Recordsの◀もどるは`onBack`propで受ける
    - ★**バグ発見・修正**: `switchProfile`（こうたい）等が`home`をクリアせず**プロファイル切替後に部屋状態が次の子に漏れる**→`pickProfile`/`handleCreate`/`switchProfile`/`doDeleteProfile`に`setHome(null)`追加。こうたいは段階①では部屋に残置（段階②で港へ移す）
    - **残り: 段階②**（起動時プロファイル選択＝島の入口/港・港交代・こうたいを港へ）＝**Chatから 港背景＋港/ふねアイコン の受領が必要**／**段階③**（ヘッダーきろく削除・子は日記・保護者はマップ下おうちのひとへ）＝**Chatから 各モードガイドの集約構成版 の受領が必要**
  - **ブラッシュアップ 第1波 1-1〜1-4 完了（v2.3-b1a〜b1d）**:
    - **b1a メモ02 名前の一貫性**: 名前を3階層で分離（第1層=マップ短縮ラベル`short`／第2層=ポップアップ場所名`place`／第3層=中のゲーム名は各画面持ち）。場所名を`AREAS`(WorldMap)/`POWERS`(powers.js)に集約。ポップアップは「**◯◯ へ いく**」に統一（文言最終確定＝当初「はいる」から「へ いく」へ・ボタンは「▶ いく！」）。場所名: クイズのひろば/おえかきのへや/そだちのもり/おみせ/おうち/タイピングタワー/パズルのしま/バトルのアリーナ
    - **b1b メモ03 ステージUI整理**: ①難易度をタブ化（3等分・面積圧縮）②難易度★を撤去し**色分け＋言葉**に（やさしい=緑`#6BCB77`／ふつう=黄`#FFD447`／むずかしい=濃橙`#FF9F43`・**赤は失敗色として不使用**・`DIFFICULTIES`/`QUIZ_DIFFS`に`color`集約）③戻る導線を「**◀もどる」1つ・1階層ずつに全11画面統一**（`common.jsx` Headerを`onHome`→`onBack`化・🏠/🗺️マップの二重ボタン廃止・バトルは「◀にげる」温存）。**なかまずかんの2階層飛びバグ修正**（go("home")→go("myhome")）。**評価（クリア成績）の★1〜3は温存**（難易度★とは別物・ロジック不変）
    - **b1c ワールドマップF2**: ポップアップにやさしい演出（背景`fadein`＋パネル`softpop`・文言不変）／アイコンに`mapfloat`（ふわふわ・**index別ディレイでばらす**・ロック中の火山は静止＝休眠感）／家アイコンは`building_home.png`で現状維持（**背景に家は無く画像化不要**）／Lv3解放通知は既存App.jsxトースト流用／未使用`iconTyping` import除去。座標微調整（クイズ/タイピング）
    - **b1d そだったちからF2**: `storage.js`に`powers:{prev:{}}`追加（前回%保存・後方互換マージ・往復試験に追加）。そだちのもりを開いた瞬間、前回より**伸びたちからを祝う**（植物`growpop`＋`sparkle`＋「そだった!」・**段階アップは`big`＋リボン＋fanfare**）。**初回は静かに基準化**（prev空なら祝わず現在値保存＝既存プレイヤーが5つ一斉に育つ事故を防止）。演出後prev更新＝再祝いなし。詳細に「**▶ 場所名 へ いってみる**」導線（メモ02の場所名・遷移／戻りは◀もどる1つに合流）
    - **タイピング座標 最終微調整（この締めで）**: タブレット横でタイピングラベルとおうち建物が4px接近していた件を、**typing `left: 48→46`**（2ポイント左）で解消。モバイル/タブレット縦横で重なりなし再確認（typing↔おうちギャップ 8px）
    - **第1波の2本の芯 達成**: ①メニューを世界にする（場所名の世界観・ポップアップ/ふわふわ演出・そだったちからの祝い）②遊びやすく（◀もどる1つに統一・難易度タブ整理・色分け）
  - --- 以下は第1波以前（v2.2-W1g まで） ---
  - P0 基盤（Pages公開・マルチプロファイル4人・書き出し/読み込み）
  - P1 相棒モンスター（3タイプ×3進化・XP・ずかん・3DルックSVG）
  - P2 パズル162面＋1枚絵島マップ（昼/夕/夜切替）＋難易度3段階
  - P3 クイズ204問（5カテゴリ×3難易度・正解一意の機械検証）
  - P4 タイピング3段階（NFAローマ字エンジン・両式受理・テスト128件）
  - UI磨き（丸ゴシック統一・あそびかた折りたたみ・3ゾーン操作・画像アイコン・保護者ガイド島1〜3）
  - **道A完了**: 162面を選抜方式で再生成し なめらかな難易度カーブ（verify統合・全18セル違反0）＋迂回解放（島内1面飛ばせる）＋救済ヒント
  - **救済ヒント改善完了（A2）**: 💡ヒント=島の一般論（自分で押す）／💪救済=その面の最短解`sol`の段階開示（2失敗=先頭1〜2手・3失敗=前半・4失敗=全手順＋スキップ案内）。重複解消済み
  - **確認モード完了（A3）**: 開発・実機確認用の隠し機能。保護者ゲート（記録→👪おうちのひとへ→「3×4」）の中のスイッチで全パズル面を一時解放。App側のセッション限りフラグ（localStorage非保存＝リロードでオフに戻り子どもの記録に残らない）。実装は `unlockAll` を App→Puzzle/Records へprops伝播。パズルの島・面の2ロックを迂回＋解放中バナー表示。クイズ・タイピングは元からロックなしのため対象外
  - **UI改善（A4）**: ①あそびかた(`HowTo.jsx`)を全モード既定で閉じる＋閉じてる間だけ「👆おしてね」誘導（bounce）＋開くと下部に「とじる」。②おうちの方へ(`ParentGuide.jsx`)を折りたたみ→**モーダル**化（×/とじるで閉じ・操作ボタンを押し下げない・解きかけ保持）
  - **もしも島（やさしい）段階設計（A5）**: 入口が全面「🔁くりかえし＋🧠」壁沿い（回数9等）で概念的に重かった問題を、**導入面方式**で解決。9面のparランプ 1→2→3→3→3→4→4→4→5、先頭2面は「🧠を1回だけ使う導入面（teach:true）」、入口の🔁回数は3程度に抑制。**もしも easyのみ差し替え・他153面は不変**。生成=`tools/gen-moshimo-easy.mjs`、`verify.mjs`に導入面検定＋全面sol妥当性チェックを追加。**救済3層化**（`Puzzle.jsx`）: 第1層=導入面／第2層=1回失敗から「つぎに置くブロックをパレットで光らせる」＋段階的な文字ヒント／第3層=3回失敗で「🔑こたえをみる」ボタン（押して初めて全手順・自動表示しない）
  - **ワールドマップ 建物イラスト化＋位置微調整（v2.2-W1d/W1e）**: W1dで6エリアを時計方向指定で微調整。W1eで**クイズ=building_quiz.png（電球ドーム）/タイピング=building_typing.png（紫の鐘楼）/おうち=building_home.png（赤屋根の家）**に差し替え（`AREAS`に`tall:true`＝縦長なので表示82%・通常アイコンは62%）。おうちは絵文字🏠→建物画像に。座標も再微調整（クイズ左上・タイピング/おえかき/ちから上・おうち右）。⚠️座標調整は毎回ユーザーが実機を見て時計方向＋量で指示→Codeが`AREAS`のleft/topを±する運用。空き地中心はグリッドoverlay(eval注入)で実測できるが、ユーザーの実機判断が最終
  - **ワールドマップ 座標の再微調整（v2.2-W1f/W1g）**: W1eの建物差し替えに続く座標のみの調整（`WorldMap.jsx` の `AREAS` 定数だけ・新機能なし）。累積で **クイズ top28→24（上へ4）／タイピング top37→51（下へ14）**。他6エリアは不変。上記の「実機を見て指示→`AREAS`を±」運用の反復。⚠️この2回はコミットメッセージが `deploy` のみで台帳未記録だった→W1g同期時に遡って記録（教訓: 座標微調整でも deploy 時に一言 version と内容を残す）
  - **ワールドマップ アイコンのサイズ・中心合わせ（v2.2-W1c）**: イラストを小さく（62%）＋イラスト＋ラベルを縦組みで空き地の円内に収める。座標`AREAS`を**空き地の円の中心**へ実測合わせ（デバッグ用に一時グリッド overlay を eval注入して%を計測→8エリア全部調整）。円からのはみ出し・重なりを解消（PC/モバイル幅で確認）。⚠️座標を再調整するときは同手法（グリッド overlay）で空き地中心を測るのが速い
  - **ワールドマップ アイコンの馴染ませ（v2.2-W1b）**: 白い丸台紙・枠を撤去し透過イラストを島に直接配置（接地影 drop-shadow で馴染ませ）。タップ範囲は透明ボタン枠(15%)で確保（透明部分もタップ可）。短い名前ラベルは下地チップ廃止→白フチ文字(textShadow)で可読性確保。押下フィードバックは`.mapicon:active`のscale。実機（PC/モバイル幅）で「ボタン感が消えた／名前が読める」を確認
  - **ワールドマップ・ホーム フェーズ1（v2.2-W1・停止ポイント）**: 旧ホーム（縦積みカード）を1枚マップに刷新（`WorldMap.jsx`・worldmap-home.webp・8エリア%座標は`AREAS`定数・拡張用空き地3つはコメントで温存）。マップ上は小アイコン＋短い名前のみ→タップでポップアップ（フル名前＋▶はいる＝誤タップ対策）。**screen名"home"を維持したため全モードの go("home") が自動でマップに戻る**（再配線は構造的に完了）。バトルはLv3ロック（暗＋🔒＋ロック文言ポップアップ）。ヘッダー（相棒・⭐・🪙・きろく・音）維持。**「わたしのおうち」ハブ**（`MyHome.jsx`: プロフィール・相棒・ずかん/きろく/こうたい導線）。旧`Home.jsx`削除。座標はブラウザ照合で微調整済み（おえかき/ちからのラベル重なり解消）。8エリア全遷移＋戻りを自動テストでPASS。**残り: フェーズ2**（ポップアップ演出・アイコンの生き生き感・家アイコン画像化・タブレット縦横レスポンシブ確認）
  - **そだったちから フェーズ1（v2.2-W1に同梱）**: 5つのちから（じゅんばん/くりかえし/かんがえる/キーボード/つくる）を植物5段階（grow_0〜4.png・しきい値0/10/35/65/90%・称号はじめ→みならい→じょうず→あとすこし→はかせ＝`src/data/powers.js`に集約）で見える化。ワールドマップ「ちから」→専用画面（`Powers.jsx`＋`PowerPanel.jsx`・タップで詳細モーダル: 大きな植物・称号・%控えめ・「○○でそだつよ」）。到達度は既存計算と同式・新規データ収集なし。**残り: フェーズ2**（「そだった！」差分演出＋前回%保存＋詳細から島への導線）
  - **UI改善3点（v2.1-P6f）**: ①**島に学年の目安**（`islands.js`に`grade`＝帯A/B/C: 島1-3=1〜2年・4-5=3〜4年・6=5〜6年）。マップの各拠点ラベル下に🎓控えめ表示（ロック島も見える＝挫折防止）＋島ヘッダーに「🎓○年生むけ（めやす）」。ロックはしない。②**バトルをLv3で解放**（`battle.js` `BATTLE_UNLOCK_LEVEL=3`/`battleUnlocked()`）。ロック中ホームのバトルカードは🔒＋「あいぼうが Lv3に なると あそべるよ・いまLv○」で非活性。Lv3到達で⚔️トースト通知（App.update内・levelup分岐に追加）。既存Lv3+は最初から解放（レベル判定のみ・マイグレ不要）。App側routeも二重ガード。③**未取得バッジに取得条件を薄く表示**（`Dex.jsx`＋`Records.js`きろくタブ: 取得=カラー＋名前／未取得=❔＋descを薄グレー）。全て既存judgeそのまま・verify全PASS
  - **クイズ難易度再設計＋プール大量化（v2.1-P6e・Fable5で量産）**: 実機FB④⑤対応。**5カテゴリ全部をメタ付きテンプレート生成に統一**（204→**336問**・各セル18〜26問・5問シャッフルは既存のまま＝画面コンパクト維持）。難易度の機械基準: きまり=AB→AAB/ABC/AABB→変則4要素・とび先・こわれ探し／じゅんばん=3ステップ端→4-5ステップ端→穴うめ・直前依存・正順選び／なかまわけ=カテゴリ軸→性質軸(同カテゴリ内)→**抽象軸4択**（3択では曖昧性を構造排除できないため）／ロボット=1概念→2操作→くりかえし・3操作／よみとり=順次→分岐→ループ回数。**verifyに難易度タグ=実難易度の照合を追加**（`expectedDifficulty()`）＋なかまわけは**全タグ軸照合**で軸競合を機械排除（りんご・トマト・いちご事故防止）＋じゅんばん誤答は「問われた位置より後ろのみ」。素材データ（因果チェーン27本・タグ辞書70個）=`tools/quiz-data.mjs`（人手品質保証の場所・書き起こし基準の適用先）。旧書き起こし84問(`quizzes-fixed.js`)は引退・削除。記録キーは`カテゴリ:難易度`のまま後方互換・バトル出題も新プールに自動接続。verify下限を18問/セル・総数300に引き上げ
  - **P6 フェーズ2 コイン＋ショップ＋きせかえ（v2.1-P6d）**: 🪙コイン=学習でのみ貯まる（レート集約=`growth.js` COIN: パズル新規★×2／クイズ正解×1／タイピング3+ベスト更新5／おえかき2・1日上限6/`log[d].artCoins`／バトル勝利5/8/12）。**導入時「いままでのがんばり」一括換算**（`grantLegacyCoins`・プロファイル選択時1回・`coinsGranted`フラグ・演出オーバーレイ）。ヘッダーに🪙常時表示。**ショップ**（`Shop.jsx`・Home入口=icon_shop.png・App route "shop"）: アイテム4種（各3個上限）＋**きせかえ**（`COSMETICS`in battle.js: 飾り5種30〜80＋背景2種50・自作CSSグラデ・ガチャなし定価）。買うと即装備・トグルで着脱。**反映**: 相棒カード(`PartnerCard` deco)＋バトル画面（相棒に飾り＋背景差し替え `equippedBgCss`）。**バッジ+3**（⚔️はじめてのしょうり/🏟️バトルマスター9体/🛍️おかいものデビュー=`shopUsed`）→計29個。**ずかんに「たおした あいて」**（`Dex.jsx`・敵9体・討伐=カラー/未討伐=シルエット）。セーブ追加=coins/coinsGranted/items/cosmetics/shopUsed（後方互換マージ・**往復試験に全項目追加しPASS**）。⚠️`badges.js`が`battle.js`経由で画像importするようになったため node直importは不可（roundtripのBADGES孤児チェックは削除）
  - **かいしん予兆＋進化演出＋成長調整（v2.1-P6b/P6c）**: ①かいしんを**事前判定→予兆から分岐（推奨A）**: 正解確定時に判定し、突進前に相棒が光って構える溜め(charge 0.68s＋⚡立ちのぼり＋「なにか くるぞ…！」)→大きい突進(--lsc 1.22)→特大💥＋critpop＋強ゆれ。タイムライン実証: charge=0ms→lunge=684ms→命中=1007ms。これで**フェーズ1.5完成**。②**進化演出をバトル並みに**（`EvolutionOverlay.jsx`全面改修）: 溜め(1.6s・だんだん強く光る)→変身(光の渦spinlight＋白シルエットがmorphしながら**フォルムがfrom→toに変わる**)→登場(画面白フラッシュ＋slamバウンド)→祝福(おめでとう＋紙吹雪14片＋ファンファーレ)。約3.8秒でボタン表示・音はOverlay側が段階に合わせて鳴らす（App側のevolve音は移管）。③**成長を+25%減速**（`growth.js` xpToNext 12+4(l-1)→15+5(l-1)。Lv5到達72→90XP・Lv12到達352→440XP。バトルでXP源が増えた分の緩和・1箇所で再調整可）
  - **P6 クイズバトル フェーズ1＋1.5（v2.1-P6a）**: ⚔️バトルモード公開。ハート制（正解=攻撃1dmg/不正解=ハート減・ハート3）・敵9体を難易度別に順解放（HP3/4/5）・出題=既存204問の難易度別プール・かいしん(Lv1=5%→Lv12=25%・2dmg)・勝利XP＋敵ずかん記録(battle.defeated/best)・敗北罰なし。**演出フルセット（フェーズ1.5・バトル演出-指示.md準拠）**: 背景3枚を難易度で出し分け（草原/夕日闘技場/夜の王座）・相棒左下×敵右上の切り抜き構図＋接地影・アイドル揺れ・突進(lunge)→白フラッシュ＋💥＋画面ゆれ→HPバー滑らか減少・ハートがパリンと割れる・かいしん文字ポップ＋強ゆれ・勝利=敵ダウン→ジャンプ→「かった！」→キラキラ・敗北=しょんぼり＋励まし。**アイテム4種の演出も実装済み**（🧃回復/👓選択肢消し/💪2倍オーラ/🛡️バリア・1問1個・`items:{}`予約済み）※購入はフェーズ2のショップから＝それまで実機では非表示。数値は`src/data/battle.js`に集約。CSSはkeyframe＋React段階制御・reduced-motion対応・アニメ中入力ロック。**残り: フェーズ2（コイン＋ショップ＋きせかえ＋バッジ3個＋敵ずかん表示＋往復試験）**
  - **P5 きろく・バッジ・ずかん拡張（v2.1-P5）**: バッジ 14→**26個**（`src/data/badges.js`。既存維持＋12追加: 島4〜6マスター🌈🧠🏰／難易度🥈ふつう7面・🥇むずかしい7面・💎むずかしい★3を5こ・🎖️どれかの島を3難易度／⌨️🎹⚡タイピング(⚡は20字/分)／📚むずかしいクイズ5種満点／🏛️作品10）。全て既存データから自動判定（helper: `clearedInDiff`/`star3InDiff`/`islandAllDiffDone`/`typingBestKpm`）。**ずかん(`Dex.jsx`)にバッジコレクション表示**（取得=金＋名前／未取得=❔＋？？？・相棒ずかんと同じ集める演出）。**保護者画面(`Records.jsx`)に「難易度べつの ようす」（現在の子・易/普/難の到達度＋★3面数）と「お子さんべつの ようす」（複数プロファイル時のみ・App→Recordsに`profiles`渡し）**を追加。既存の学び翻訳・14日グラフは維持。**書き出し/読み込みラウンドトリップ試験**（`tools/test-roundtrip.mjs`・localStorageシムで往復・新バッジ含む全項目一致）で P5完了条件クリア
  - **「おうちの方へ」全モード展開（A7）**: パズル島4〜6・クイズ5カテゴリ・タイピング（共通＋段階別補足）・おえかきに、既存と同じモーダル（うすむらさき・保護者むけバッジ）で追加。文章はChat作成の原稿を `src/data/parent-guide.js`（**正本**）に取り込み（原稿ファイルは取り込み後に削除・一本化）。キーだけアプリ実IDに合わせた: クイズ「ずのよみとり」=`yomitori`（原稿flow）・タイピング段階=`kotoba/tanbun/bunshou`（原稿stage1/2/3）。`ParentGuide.jsx` を汎用化（`island`または`guide`直渡し＋`extra`=段階補足📍）。全モードで解きかけ保持を実機確認済み
  - **段階設計の全島展開（A6・Fable 5で量産）**: 実機成功を受け、診断→重い入口（🧠入りループ多周回）が集中する**8セル72面を再生成**＝島3 normal/hard・島4全・島5全（`tools/gen-staged.mjs`・面ごとexact-par直接探索）。導入面: 島3normal×1・島4easy×2・島4normal×1（hardは導入面なし・小回数キャップのみ）。キャップ=🧠入りループ回数を入口ほど小さく（3〜5）、⬆のみのループは+3緩く（島5の圧縮面を殺さない）。**島1（🔁なし）・島2（move主体小回数）・島6（🧠なし小回数）は診断の結果すでに軽く、対象外＝既存面保持**。SPEC par下限を島3normal[4,8]・島4easy[3,6]・島4normal[4,7]に調整。verify の導入面検定を島別concept（TEACH_NEED）に一般化。島3hardの高par(10-11)は🧠+🔁圧縮下では実在せず 7→9 ランプに（実分布準拠）
- 検証体制: `npm run verify` ＝ パズル162面（★3最短＋難易度カーブ）＋クイズ204問（正解一意＋ループ回数表記禁止）＋ローマ字128件。FAILだと `npm run deploy` で公開されない

### 未完了タスク（次セッションはここから）

1. ~~段階設計の展開~~ → **A6で完了**（島3 normal/hard・島4全・島5全の8セル72面。島1・2・6は診断の結果すでに軽く対象外）。ユーザーの実機確認待ち
2. ~~全ステージ一時解放モード~~ → **A3で完了**（確認モード）
3. ~~「おうちの方へ」の展開~~ → **A7で完了**（全モード。ユーザー実機確認待ち）
4. ~~P5 きろく・バッジ・ずかん拡張~~ → **v2.1-P5で完了**（バッジ26個・ずかんにバッジ・保護者画面プロファイル別/難易度別・往復試験済み）。ユーザー実機確認待ち
5. **P6 クイズバトル**（フェーズ1/1.5/2=v2.1-P6dまで公開済み・ユーザー実機のバランス確認待ち）: 残りは**フェーズ3=全体通し確認のみ**（絵は全て差し込み済み）。バランス（コインの貯まり方・アイテム価格・一括付与の体感）をユーザーが確認後、必要なら`growth.js`COIN・`battle.js`ITEMS/COSMETICS価格を1箇所調整
6. **高学年拡張**（未着手・着手前にChatで設計）: 帯B 変数・イベント／帯C 自由制作・デバッグ・コード橋渡し

### 次の一手

1. **v2.3-b3d の実機確認**（第3波全体＋今回のb3d2点修正）: ⓐ探検家選択の移行体験 ⓑショップ購入フロー ⓒ着せ替えの見た目 ⓓ**吹き出しUIの形・大きさ**（今回roomhot→bubbleに変えた・微調整あれば`theme.js`の`.bubble`）ⓔ**メガネのアンカー**（16.5%へ調整済み・まだズレるなら`dressup.js`のface anchorをあと1〜2%）ⓕ女の子のバンダナと髪の前後関係
2. ✅**正本4ファイルの上書き完了**（2026-07-08）: project-charter.md / roadmap.md / feature-spec.md / education-curriculum.md を、Chat作成の更新版（第1〜3波＋着せ替え編を反映）に差し替え。**取り込み時に1点訂正**: feature-spec.md §2が新フィールド`dressupOwned`と記載していたが、実装は`cosmetics.owned`のまま（フィールド名は旧「相棒の飾り」時代を流用・中身だけ差し替え）→ 実装に合わせて文言を訂正してから採用。他3ファイル・feature-specの他記述は実装と整合確認済み。版ズレ解消
3. **正本更新後**: コインの貯まり方を見て**メモ06-C**（着せ替え価格の本決め・コイン価格上げ・導入時一括付与471枚の見直し＝`growth.js`COIN/`dressup.js`価格）を判断 → その後**第4波**（メモ08おえかき大改善・メモ07方眼メモ・メモ06-Aバトル歯応え＝`brushup/03_code_guide_by_wave.md`第4波）。おえかき改善時は`~/progland-brushup/js統合待ち/art_guide_revised.js`を統合
   - **未確認の申し送り（過去分）**: P6e クイズ難易度差・P6f 学年目安/バッジ条件（P6dコインバランスは上記メモ06-C判断と統合）
2. 更新の出し方: `npm run deploy` 一発（verify→build→docs/→push。devサーバは止めてから）
3. 調整値の場所: 経験値・レベル曲線=`src/growth.js` ／ パズル生成基準=`tools/criteria.mjs`（par範囲・採用検定）／ クイズ品質基準=`tools/quiz-criteria.mjs` ／ モンスター絵=`src/data/monster-art.js` ／ 島拠点座標=`Puzzle.jsx` の `ISLAND_POS`

### ファイルの地図

| ファイル | 役割 |
|---|---|
| `progland-実装指示書.md` | **指示書の正本（第2版・2026-07-04改訂）**。第1版はgit履歴。承認なしに編集しない |
| `worldmap-指示.md` | P2追補: 島マップ1枚絵化の指示（親: 実装指示書） |
| `worldmap-難易度別-指示.md` | P2追補2: 難易度別マップ背景（昼/夕/夜）の指示 |
| `icon差し替え-指示.md` | P3追補: 絵文字→オリジナル画像差し替えの指示 |
| `icon_typing差し替え-指示.md` | P4追補: タイピングアイコン差し替えの指示 |
| `src/data/howto.js` | 各モード・島・カテゴリの「あそびかた」説明文 |
| `src/components/HowTo.jsx` | あそびかた折りたたみ部品（A4で既定閉じ＋「おしてね」誘導） |
| `src/components/blocks.jsx` | 積み木ブロック風の命令ブロック（パレット＋命令枠共通・くりかえしC字形・A5でパレットhighlight追加） |
| `tools/curve.mjs` | 難易度カーブ検証（par単調・段差・新概念1つずつ・verify統合） |
| `tools/curate.mjs` | 選抜つき生成（プール→par分布追従の均等選抜）＝量産の核 |
| `tools/solve.mjs` | ソルバー（最短par＋最短解sol抽出・メモ化） |
| `tools/gen-moshimo-easy.mjs` | もしも easy 9面の段階設計生成（A5）。`moshimo-段階設計-案.md`=承認済み設計 |
| `tools/gen-staged.mjs` | **段階設計セルの正本ジェネレータ（A6）**: 島3normal/hard・島4全・島5全の8セルを再生成（導入面＋回数キャップ＋parランプ）。⚠️`generate.mjs --write`全再生成は段階設計を失うので使わない |
| `src/data/badges.js` | バッジ26個の定義と自動判定（P5）。追加はここに `check(save)` を足す。既存IDは消さない（獲得済みは和集合で保持） |
| `tools/test-roundtrip.mjs` | 書き出し/読み込みの往復試験（localStorageシム）。セーブ項目を増やしたら必ずここに検証を足して実行 |
| `p6-battle-shop-設計.md`／`p6-実装指示書.md` | P6の設計正本と実装手順（フェーズ1/2/3・停止ポイント） |
| `バトル演出-指示.md`／`画像対応.md` | P6追補: 演出フルセットの指示（フェーズ1.5）／敵9体・アイコンのファイル対応表 |
| `src/data/battle.js` | バトル設定の集約（敵9体・HP・かいしん率・XP・コイン/アイテム定義）。数値調整はここだけ |
| `src/components/Battle.jsx` | バトル画面（選択→シーン演出→勝敗）。演出はCSS keyframe＋段階制御 |
| `src/components/ParentGuide.jsx` ＋ `src/data/parent-guide.js` | 「おうちの方へ」モーダル（A7で全モード展開: パズル島1〜6・クイズ5カテゴリ・タイピング・おえかき）。**`src/data/parent-guide.js` が唯一の正本**（Chat原稿は取り込み済みで削除。キー名は原稿flow/stage1-3→実装yomitori/kotoba等に変換済み） |
| `quiz-書き起こし基準.md` | クイズ素材の人手作成基準（P6e以降は `tools/quiz-data.mjs` の素材に適用） |
| `tools/quiz-data.mjs` | クイズ素材の正本（因果チェーン・なかまわけタグ辞書・絵文字プール・フロー素材）。問題を増やす＝ここに素材を足して `node tools/quizgen.mjs --write` |
| `project-charter.md` | 設計書: 案件憲章（Chat側作成） |
| `education-curriculum.md` | 設計書: 学習カリキュラム設計（Chat側作成） |
| `roadmap.md` | 設計書: ロードマップ（Chat側作成） |
| `feature-spec.md` | 設計書: 機能仕様（Chat側作成） |
| `progland-handoff.md` | 本台帳（引き継ぎの入口はここ1本） |
| `CLAUDE.md` | Code用の案件前提・固有注意 |
| `brushup/` | **ブラッシュアップ設計パックの新規ドキュメント**（2026-07-06振り分け）。実装の波の地図=`03_code_guide_by_wave.md`（波ごとの使うファイル＋指示文）・`01_roadmap_implementation.md`・`02_handoff.md`、新規実装指示=memo04(おうち部屋)/memo05(ショップ)/worldmap_home_design。配置経緯は`brushup/_配置メモ.md`。※P6等の重複.md9本は既存日本語名ドキュメントが正本のため取り込まず |
| （保管・リポジトリ外） | メモ08/09の統合待ち.js（`art_guide_revised.js`=第4波おえかき・`parent_intro.js`=第2波③理念文・`parent_guide_full.js`=参考）は `~/progland-brushup/js統合待ち/` に保管（Downloads一括削除の対象外・リポジトリ外）。波が来たら統合。新規画像 room-home.webp/shop-interior.webp は `src/assets/` へ配置済み |
| `programming-land.jsx` | v1移植元。参照専用・編集しない |
| `src/data/` | ステージ・クイズ・おえかき・バッジのデータ（ハードコード禁止の受け皿） |
| `src/engine.js` | パズル実行ルール（UIとソルバーの共通仕様） |
| `src/storage.js` | localStorage保存・プロファイル管理・書き出し/読み込み |
| `src/version.js` | バージョン表示（リリースごとに手で上げる） |
| `tools/verify.mjs` | ステージ全数検証ソルバー（`npm run verify`） |
| `docs/` | ビルド成果物（Pages配信元。`npm run deploy` が自動生成） |

### 決定事項（要点。詳細は指示書第2版を参照）

- [DECISION] Vite + React 静的ビルド。**配布はGitHub Pages公開**（2026-07-04に単一HTML配布から変更）。アカウント kanda-houtokukai・無料・push→Actions自動デプロイ・アプリ内バージョン表示
- [DECISION] **マルチプロファイル最大4人**。記録・相棒・バッジは人ごとに完全分離、書き出し/読み込みもプロファイル単位
- [DECISION] **育成ゲーム化**: オリジナルモンスター（実在IP禁止・自作SVG）。相棒3体から1体→経験値→3段階進化。島マップ・ずかん・クイズバトル（P6）
- [DECISION] 難易度3段階を全モード共通。記録は難易度別、解放条件は難易度内で完結
- [DECISION] フェーズ再編（第2版）: P0きばん → P1そだてる基盤 → P2パズル増量＋島マップ → P3クイズ増量 → P4タイピング → P5きろく・バッジ・ずかん拡張 → P6クイズバトル。**以後フェーズは番号＋名前で呼ぶ**
- [DECISION] P2ステージは手作業で作らず、ジェネレータ＋ソルバー自動検証（`npm run verify` 全数PASS）で量産
- [DECISION] やらないこと: オンライン対戦含む通信機能・広告・外部連携、サーバ・DB（拡張余地のみ残す）、実在キャラ素材/名前、UIトーン刷新

### 生きている注意事項

- ★ dev サーバ稼働中に `npm run build` しない（ビルド破損の既知事故と同型）
- ★ AudioContext はユーザー操作後に初期化（v1は対策済み・踏襲）
- ★ 保存データはバージョン番号つき、`{...newSave(), ...parsed}` のデフォルト値マージ。形式変更時はマイグレーション同梱
- ★ プレビュー成功≠完成。フェーズ完了時は公開URLで実機確認（PC Safari＋タブレット）。Pages反映はpush後1〜2分・シークレットウィンドウで確認
- ★ セーブスキーマは相棒・経験値・ずかん・難易度別記録をP0から予約しておく
- 中間報告ポイント: P0完了時・P1完了時・P2ジェネレータ＋ソルバー完成時（量産前）・各フェーズ完了時

---

### A6で得た教訓（全島展開）

- ★ **展開の前にまず診断**: 「重い入口」の実体は「🧠入りループの多周回」で、smart系の島（3・4・5）にだけ存在した。島1（🔁なし）・島2（move主体）・島6（🧠なし）は構造的に起きない＝差し替え不要と分かり、対象を18セル→8セルに絞れた。パターンの正体を先に特定すると展開範囲が半分になる。
- ★ **プール方式は par の端が薄い**: 広いpar範囲でプールを作ると高par面が集まらない（島3normal par8で枯渇）。段階設計の量産は「面ごとに exact-par 直接探索」（gen-moshimo方式）が確実。
- ★ **SPECのpar上限は理論値でなく実分布で**: 島3hardのpar10〜11は🧠+🔁の圧縮が効く限り実在しない（総当りでも出ない）。ランプは実際に生成できる分布（7→9）に合わせる。

### A5で得た教訓（もしも島の入口）

- ★ **parは概念的な重さを表さない**。もしも島は入口から全面「🔁くりかえし＋🧠」の壁沿い（回数9等）で、par3でも大人が詰まった。原因は生成基準 `criteria.mjs` の島3 accept が「🧠を必須」に課していたこと。P2教訓のとおり🧠は🔁と組まないと縮まらないため、「🧠必須」＝必ず🔁併用＝重い、が構造的に生じていた。
- ★ 対策＝**導入面（teach:true）**: 島の先頭1〜2面だけ「🧠必須」検定を免除し、小さく開けた盤面で🧠を1回だけ使う par1〜2 の面にした。★3の最短が素直な解になるので、子どもの素直な解が報われる。`verify.mjs` は導入面を「手本解でクリア＋最短=par＋🧠使用」で検定する（＋全面に sol 妥当性チェックを追加して硬化）。
- ★ **1セルだけ差し替える運用**: 他の153面を一切動かさないよう、もしも easy 9件だけを再生成して splice（`gen-moshimo-easy.mjs`）。共有RNGを回す `generate.mjs` 全再生成だと下流の島まで変わるため、部分改修は専用スクリプトで該当セルだけ書き換える。

### P4後の教訓（クイズ図）

- ★ 「正解が一意に定まる（別解なし）」の検証だけでは、**図の読みにくさ（曖昧な表記）は検出できない**。くりかえしの中身に回数を書くと外側と二重になり、答えは一意でも人が読めない。生成物は「機械が解けるか」だけでなく「人が図から一意に読めるか」の観点でも基準化する（`quiz-criteria.mjs` にループ回数表記の禁止ルールを追加）
- ★ くりかえしを図示するときは「中身＝1回分の動作だけ・複数回は動作を並べる」。回数は外側のループ表記にのみ書く

### P4で得た教訓

- ★ ローマ字判定は「ん」で必ず曖昧になる（san の n は完成か nn の途中か未確定）。解釈の集合を同時に保持するNFA方式で解決。`src/typing/romaji.js` が正本、テストは `tools/verify-typing.mjs`
- ★ Reactのkeyに `Date.now()` を使うと親の再描画のたびにコンポーネントが作り直される（結果画面が消えるバグの原因）。keyは選択状態など安定値のみ使う

### P3後の教訓

- ★ **`git add -A` の巻き添えで中間物が混入した実例**: P3コミットに `files.zip`（0.3MB）が入りGitHubに載った（公開docsには出ず実害なし）。P3.2で削除・`.gitignore`に`*.zip`追加。以後、素材zip等を作業ディレクトリに置く運用なら add 前に `git status` を確認する
- ★ 指示書の実装方法が実素材に合わないことがある（robot.pngは正面立ち姿→「回転で4方向」だと横倒し/逆さになる）。実素材を見てから方法を確定し、ズレはユーザーに撤回提案する（この件は現状の▶矢印方式維持で決着）

### P2で得た教訓

- ★ **🧠かべ？ブロックは🔁くりかえしと組まないと最短ブロック数を縮められない**（まっすぐ並べる限り実行時の動きが固定され、通常ブロックで等価置換できる）。「もしもの島」に🔁を入れたのはこのため
- ★ 総当たりソルバーは par7 前後が限界。状態メモつき反復深化に書き換えて8×8・par12まで厳密検証可能になった（162面0.6秒）。新旧ソルバーは15面交差検証で一致確認済み
- ★ バッジ判定を「毎回全再計算で置き換え」から「和集合」に変更（ステージ構成変更で獲得済みバッジが消える事故を防ぐ）

### P0で得た教訓

- ★ 保存済みGitHubトークンには workflow 権限がない → Actions のワークフローファイルは push できない。デプロイは mainの docs/ フォルダ配信方式（`npm run deploy`）に決定
- ★ GitHub Pages のデプロイは「Deployment failed, try again later」でときどき失敗する（新規サイト直後に2回・運用中のP2.2でも1回発生）。対処は毎回同じ: **空コミットをpushして再デプロイ**。verify・ビルドは無関係（ローカルのdocs/が正しければ再pushだけでよい）
- ★ v1のpar値は6面で最短解より甘かった（ソルバー検証で発見・修正済み: 1-3, 2-4, 3-2, 3-3, 3-4, 3-5）。以後ステージ追加は必ず `npm run verify` を通す
- ★ ローカルのUIテストで「ブロック追加と実行を同一イベント内で行うとReactのstate反映前に実行される」現象はテスト手順の問題で、実操作では起こらない（バグではない）

## 経緯アーカイブ（必要なときだけ読む）

### 2026-07-04 各フェーズの実装詳細（現在地サマリから移設）

- **P2**: 162面（6島×3難易度×9面）自動生成＋全数検証。島構成: ①じゅんばん ②くりかえし(+🔁) ③もしも(+🔁+🧠) ④くみあわせ ⑤あたまのたいそう(最短特化) ⑥ちょうせん(大迷路)。v1の15面は引退。バッジは獲得済みを失わない和集合方式に変更。
- **P2.2**: 島マップを1枚絵ワールド（worldmap.webp・16:9・％座標）に刷新。拠点座標=`Puzzle.jsx` の `ISLAND_POS`。指示書開始値の②③は上下逆と判断し道順どおり再割当（コード内コメントあり）。
- **P2.3**: マップ背景を難易度で切替（昼/夕/夜・同一構図で座標共通・`MAP_BG` 定数）。
- **P3**: クイズ204問=生成120（きまり/ロボット/よみとり）＋書き起こし84（じゅんばん/なかまわけ・`quiz-書き起こし基準.md` 準拠）。セッション5問シャッフル・選択肢順もシャッフル。ベストは `カテゴリ:難易度` キー（v1のq1〜q4は無害残存）。
- **P3.2**: メイン絵をオリジナル画像化。盤面ロボット=robot.png（ドット絵・pixelated・正面固定＋▶矢印。回転案は立ち姿に不適でユーザー撤回）／カメ=turtle.png（上下=回転・左右=ミラー）／ホーム3モード=icon_*.png（3Dルック）。P3コミットに誤混入した files.zip を削除し .gitignore に *.zip 追加。
- **P4**: タイピング3段階（ことば40語=綴り＋次キー点灯／短文20=綴りのみ／長文15=誤打時のみヒント）。NFAローマ字エンジン `src/typing/romaji.js`（ん・っの曖昧さを解釈集合で処理・訓令/ヘボン両受理・ヒント代表は訓令式）。分かち書き表示・入力はスペースなし。記録=正確率＋字/分、きろく画面「キーボード入力」バー（60字/分=100%）。
- **P4.2**: タイピングアイコン画像化（4モード全て画像に。機能アイコン絵文字は据え置き）。
- **P4.5**: パズル操作を3ゾーン分割=①くみたてる（積み木ブロック風 `blocks.jsx`・くりかえしはC字囲み）②うごかす（緑地の下部固定・fixed＋ResizeObserver余白）③ヒント（ヘッダーへ）。
- **P4.6**: クイズのくりかえし図の二重回数表記を修正（中身は動作を並べる）。`quiz-criteria.mjs` にループ回数表記禁止ルール追加・9問修正。
- **P4.7**: パズルプレイ画面に「👨‍👩‍👧 おうちの方へ」（`ParentGuide.jsx`＋`data/parent-guide.js`・島1〜3・既定閉じ・解きかけ保持）。
- **道A（A1）**: 162面を選抜方式（`tools/curate.mjs`: プール生成→par分布追従の均等選抜）で再生成。カーブ検証 `tools/curve.mjs` をverify統合（par非減少・段差≤2・新概念1つずつ・島入口/島またぎ）。迂回解放=クリア数+1面まで。解抽出メモ化で68秒→1.6秒。
- **A2**: 救済ヒントを「その面の最短解の段階開示」専用に（一般論は💡ヒントのみ）。島2/島3代表面で重複なし・答え到達を目視確認。

### 2026-07-04 立ち上げ〜指示書第2版

- Chat側で v2 指示書（第1版）が完成し、Code側で §7-3 に従いリポジトリ作成・CLAUDE.md・本台帳を初期化。指示書本文を正本として `progland-実装指示書.md` に保存。
- `programming-land.jsx` は当初未配置 → ユーザーが配置し、全1,144行の棚卸しを実施。
- 棚卸しの要点: 保存API使用は3か所のみでlocalStorage置換は容易。効果音はWeb Audio自前生成で自動再生制限対策済み。ステージ15面・クイズ20問・バッジ14個はすべて分離可能。★発見した問題: ①日付がUTC基準（朝9時前が前日扱い）②「星45個」等のハードコード ③GoogleフォントのCDN読み込み（オフライン時はフォールバックで実害なし→維持と決定）。
- 棚卸し報告への返信で大型の設計変更を受領（Pages公開・マルチプロファイル・育成ゲーム化・P5クイズバトル追加）→ 指示書を第2版に改訂し、フェーズをP0〜P6に再編。
